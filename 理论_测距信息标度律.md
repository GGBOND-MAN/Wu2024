# 测距信息的标度律：把攻击点从"我测到了"升级为"我证明了"

> **状态**：解析推导完成，8 个指数全部经数值 FIM 验证通过，闭式比值与数值界误差 0.8%。
> 代码：`sim/scaling_law.py`（Python）、`matlab/scaling_law.m`（MATLAB，自包含可直接运行）。

---

## 1. 核心命题

近场宽带系统中，距离信息只有**两个互斥的来源**。整条 beam-squint 脉络
（Luo2024 → Zhang2026 → arXiv:2603.16390）**只用了其中病态的那一个**。

### 来源 A：波前曲率（两篇论文的唯一来源）

距离只通过 Fresnel 二次项 $\dfrac{x^2\cos^2\theta}{2r}$ 进入模型：

$$\frac{\partial \Phi_n}{\partial r} = -\frac{2\pi f_c}{c}\left(1 - \frac{x_n^2\cos^2\theta}{2r^2}\right)$$

**关键一步**：复幅度 $\alpha$ 未知，$\partial\Phi/\partial r$ 中沿阵列**恒定**的那一项
（即 $-2\pi f_c/c$）被 $\arg(\alpha)$ 完全吸收。边缘化后只剩 $x_n^2$ 相对其均值的偏差：

$$\left.\frac{\partial \Phi_n}{\partial r}\right|_{\perp} = \frac{2\pi f_c}{c}\cdot\frac{\cos^2\theta}{2r^2}\cdot\left(x_n^2 - \langle x^2\rangle\right)$$

对 $x$ 在 $[-D/2, D/2]$ 上均匀：$\mathbb{E}[x^2]=L^2/3$，$\mathbb{E}[x^4]=L^4/5$（$L=D/2$），
故 $\operatorname{Var}(x^2)=4L^4/45$，得 $\sum_n (x_n^2-\langle x^2\rangle)^2 = N D^4/180$。

$$\boxed{\;J_r^{\text{curv}} = 2\,\mathrm{SNR}\left(\frac{2\pi f_c}{c}\right)^2\cos^4\theta\;\frac{N D^4}{720\, r^4}\;}$$

### 来源 B：时延（无人使用）

$\Phi(n,m) = -\dfrac{2\pi f_m}{c} r_n$ 关于 $m$ **严格线性，不含任何近场近似**。
单个共享的 $\alpha$ 只能吸收沿频率的**均值**，留下偏差 $f_m - f_c$：

$$\boxed{\;J_r^{\text{delay}} = 2\,\mathrm{SNR}\;N M \left(\frac{2\pi}{c}\right)^2\frac{W^2}{12}\;}$$

---

## 2. 结论：三条标度律

| | RMSE$_r$ 标度 | 随 $r$ | 随 $\theta$ | 吃什么 |
|---|---|---|---|---|
| 曲率（窄带） | $\dfrac{\lambda r^2}{N^{2.5} d^2 \cos^2\theta}$ | **$\propto r^2$** | $\propto 1/\cos^2\theta$ | **孔径** |
| 非相干宽带（Zhang2026 所属类别） | 上式 $/\sqrt{M}$ | $\propto r^2$ | $\propto 1/\cos^2\theta$ | 孔径 |
| 相干时延（本文方案） | $\dfrac{c}{W\sqrt{NM}}$ | **无关** | **无关** | **带宽** |

**这解释了此前所有的经验观测**：

- 为什么距离误差随距离急剧恶化 → $\propto r^2$
- 为什么空间平滑（$M_s = N/2$）自毁 → 曲率吃孔径，砍一半孔径损失 $2^{2.5}\approx 5.7$ 倍
- 为什么角度从来不是问题 → $\partial\Phi/\partial\theta \propto x\cos\theta$ 关于阵列中心是**奇函数**，本就与 $a$ 近似正交，边缘化不影响它
- 为什么本方案不随 $r$、$\theta$ 退化 → 时延测距与几何无关

---

## 3. 闭式比值（论文的核心公式）

$$\boxed{\;\frac{\mathrm{RMSE}_r^{\text{curv}}}{\mathrm{RMSE}_r^{\text{delay}}} \;=\; \sqrt{60M}\;\cdot\;\frac{W}{f_c}\;\cdot\;\frac{r^2}{D^2\cos^2\theta}\;}$$

四个因子各有清晰的物理含义：

| 因子 | 含义 |
|---|---|
| $r^2/D^2$ | **用户越远、阵列越小，曲率法输得越惨**（曲率需要孔径张角） |
| $W/f_c$ | 相对带宽 —— 6G 的核心趋势正是它变大 |
| $\sqrt{M}$ | 子载波数（相干积累） |
| $1/\cos^2\theta$ | 偏离法向更差 |

对 Zhang2026 所属的**非相干**类别，比值除以 $\sqrt{M}$：

$$\frac{\mathrm{RMSE}_r^{\text{incoh}}}{\mathrm{RMSE}_r^{\text{delay}}} = \sqrt{60}\cdot\frac{W}{f_c}\cdot\frac{r^2}{D^2\cos^2\theta}$$

代入其默认配置（$r{=}30$ m, $\theta{=}15°$）得 **920.6**，
而数值 CRLB 给出 **913** —— 吻合。

---

## 4. 数值验证结果（全部通过）

### 指数

| 量 | 数值拟合 | 解析预测 | |
|---|---|---|---|
| RMSE$_{\text{curv}}$ vs $N$ | −2.500 | −2.5 | ✅ |
| RMSE$_{\text{delay}}$ vs $N$ | −0.500 | −0.5 | ✅ |
| RMSE$_{\text{curv}}$ vs $r$ | +2.000 | +2.0 | ✅ |
| RMSE$_{\text{delay}}$ vs $r$ | −0.000 | 0.0 | ✅ |
| RMSE$_{\text{delay}}$ vs $W$ | −1.000 | −1.0 | ✅ |
| RMSE$_{\text{delay}}$ vs $M$ | −0.500 | −0.5 | ✅ |
| RMSE$_{\text{incoh}}$ vs $M$ | −0.500 | −0.5 | ✅ |
| RMSE$_{\text{curv}}$ vs $1/\cos^2\theta$ | 1.000 | 1.0 | ✅ |

### 闭式比值 vs 数值 CRLB（误差恒为 0.8%）

| $(r,\theta)$ | RMSE$_{\text{curv}}$ | RMSE$_{\text{delay}}$ | 实测比 | 预测比 |
|---|---|---|---|---|
| (10 m, 15°) | 2.471×10⁻¹ | 5.381×10⁻⁵ | 4,592 | 4,629 |
| (30 m, 15°) | 2.224×10⁰ | 5.380×10⁻⁵ | 41,335 | 41,659 |
| (50 m, 45°) | 1.153×10¹ | 5.380×10⁻⁵ | 214,257 | 215,934 |
| (80 m, 45°) | 2.951×10¹ | 5.380×10⁻⁵ | 548,498 | 552,791 |

---

## 5. 最有杀伤力的推论：交叉点

令两者相等，解出曲率法尚具竞争力的距离上限：

$$r_{\times} = D\cos\theta\sqrt{\frac{f_c}{W\sqrt{60M}}}$$

代入 Zhang2026 自己的配置（$N{=}256$, $f_c{=}60$ GHz, $W{=}3$ GHz, $M{=}2048$）：

| $\theta$ | $r_\times$ |
|---|---|
| 0° | **15.2 cm** |
| 15° | 14.7 cm |
| 30° | 13.2 cm |
| 45° | 10.8 cm |
| 60° | 7.6 cm |

> ### **它声明的感知区从 15 m 开始 —— 比交叉点远 99 倍。**

也就是说：**在这篇论文自己设定的整个工作区域内，曲率测距从一开始就不该被使用。**
这不是"Zhang2026 实现得不好"，而是**这一整类方法在该参数区域内注定失效**。

这一句就是 Introduction 的核心论断，也是把 comment 升格为 paper 的那一步。

---

## 6. 写进论文时的注意事项

1. **不要用 inverse crime 开场**。交叉点公式才是开场白 —— 技术性、建设性、不带指控。
   Fig. 13 的模型失配证据放到仿真章节，中性表述：
   "we regenerate the comparison under the exact spherical-wave model"。
2. **$r_\times$ 公式要作为 Proposition 给出**，附完整推导（本文档第 1 节）。
   审稿人会验算，推导必须干净。
3. **孔径悖论值得单独一段**：曲率法吃孔径，所以 Zhang2026 的空间平滑
   （$M_s=N/2$）在为了修复秩亏的同时，把自己的距离信息砍掉了 $2^{2.5}\approx5.7$ 倍。
   这是它内部自相矛盾的定量证据。
4. **角度不受影响这一点要明说**，它让攻击更锐利：不是"这方法不行"，
   而是"这方法的距离维不行，且原因是结构性的"。

---

## 7. 下一步

理论骨架已完成。按既定顺序，接下来是**多径信号模型 + 可辨识性分析**
（必须理论先行：多条路径下逐天线时延变成多时延问题，跨天线的路径关联策略
要先定清楚再写代码）。
