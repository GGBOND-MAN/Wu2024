# 测量获取：最小探测数与合并器设计

> **状态**：理论推导 + 数值验证完成。代码 `sim/acquisition.py`。
> **结论**：**T = 3 次探测即可达到相干界**，而 Zhang2026 需要 T = N = 256。

---

## 1. 为什么必须先解决这个问题

这是整篇论文**唯一的致命软肋**：

| 方案 | 感知开销 | 每次拿到什么 |
|---|---|---|
| Luo2024 CBS-Low | **2 次波束扫描** | 每子载波一个标量 |
| Zhang2026 | T = N = **256** 次顺序探测（其 Eq. 9–11） | 完整 N 维阵列快拍 |
| 本方案（朴素实现） | T = N = **256** | 同上 |

审稿人一句话即可否掉：*"你需要完整阵列快拍（256 次探测），CBS-Low 只要 2 次，这个对比公平吗？"*

我们之前把这一点作为**攻击 Zhang2026 的 A6**（隐藏 128× 开销）—— 但朴素实现继承了同样的成本，
不解决就是回旋镖。

---

## 2. 含合并器的 CRLB

用 T 个模拟合并向量后，每子载波观测 $z_m = V_m^H a_m \alpha$，参数导数变为 $V_m^H d_{i,m}$：

$$J_{ij} \;\propto\; \sum_m \Re\left\{ d_{i,m}^H\, V_m V_m^H\, d_{j,m}\right\}$$

**因此估计器能学到的关于 $(\theta,r)$ 的一切，都活在三维子空间**

$$\mathcal{S}_m = \mathrm{span}\left\{a_m,\;\frac{\partial a_m}{\partial\theta},\;\frac{\partial a_m}{\partial r}\right\}$$

**之内**。若 $V_m$ 的列张成 $\mathcal{S}_m$，则信息零损失。

注意 $\mathcal{S}_m$ 是**频率相关**的（$a_m$ 随 $f_m$ 旋转）。纯 PS 合并器频率恒定、无法跟踪；
**PS+TTD 可以** —— 这给 TTD 硬件提供了一个**信息论层面的理由**，
而整条 beam-squint 文献只把 TTD 当作"抑制斜视"的工具。

---

## 3. 数值验证：T = 2 即零损失

$N{=}256$，64 个子载波，$r{=}30$ m，$\theta{=}15°$，每阵元 SNR = 0 dB。
全阵列（$T{=}N{=}256$）基准：RMSE$_r \geq 3.044\times10^{-4}$ m。

| T | oracle 子空间 RMSE$_r$ | 损失 | 随机 PS | 损失 |
|---|---|---|---|---|
| 1 | 奇异（无角度信息） | — | 5.49×10⁻³ | 18.0× |
| **2** | **3.044×10⁻⁴** | **1.00×** | 2.88×10⁻³ | 9.5× |
| 3 | 3.044×10⁻⁴ | 1.00× | 2.60×10⁻³ | 8.5× |
| 64 | （T=2 已饱和） | — | 5.94×10⁻⁴ | 1.9× |

且在整个感知区一致成立（$r\in\{15,30,50\}$ m × $\theta\in\{0°,30°,55°\}$，损失恒为 1.000×）。

### 为什么 T 这么小 —— 与标度律直接咬合

$$d_{r,m} \approx -j\frac{2\pi f_m}{c}\,a_m$$

**距离信息本身就沿着 $a$ 方向**，靠的是系数随 $f_m$ 的变化。所以：

> **测距不需要空间自由度。空间维只用来测角度。**

$T{=}1$ 失败正是因为 $\|a_m\|^2$ 与 $\theta$ 无关 → 无角度信息 → FIM 奇异。
$T{=}2$（$\mathrm{span}\{a_m, \partial_\theta a_m\}$）补上角度维后即告饱和。

---

## 4. 关键性质：对角度敏感，对距离免疫

匹配合并器建立在**粗估计** $(\hat r_0, \hat\theta_0)$ 而非真值上时：

| 合并器角度误差 | 损失 | | 合并器距离误差 | 损失 |
|---|---|---|---|---|
| 0.01° | 1.00× | | 1 m | 1.00× |
| 0.10° | 1.00× | | 5 m | 1.00× |
| 0.50° | 2.15× | | 15 m | 1.02× |
| 1.00° | 4.55× | | 40 m | 1.06× |
| 5.00° | 26.1× | | **70 m** | **1.09×** |

**对角度极敏感（需优于 ~0.1°），对距离几乎免疫（70 m 误差只损失 9%）。**

而实验 B 已证明 beam-squint 粗估计**恰好**是：角度中位误差 **0.016°**，距离中位误差 **47 m**。

在实验 B 实际交付的精度下：

| 粗估计误差 | 损失 |
|---|---|
| 0.016° + 0 m | 1.00× |
| 0.016° + 47 m（实验 B 中位） | **1.07×** |
| 0.016° + 88 m（实验 B 最坏） | 1.11× |

---

## 5. 由此确定的获取方案

> **Stage A（1 次探测）**：一次 beam-squint 扫描 → 粗角度，精度 0.016°。
> **距离输出直接丢弃**（实验 B 已证明它是垃圾）。
>
> **Stage B（2 次探测）**：两个匹配到粗角度的合并器，距离随便猜。
>
> **总计 T = 3。**

| 方案 | T | 距离 RMSE @0 dB |
|---|---|---|
| Luo2024 CBS-Low | 2 | ~0.17 m（其 Fig. 14，2D） |
| Zhang2026 | 256 | 0.904 m（本仓库复现） |
| **本方案** | **3** | **~3×10⁻⁴ m** |

**与 CBS-Low 同量级的开销，比 Zhang2026 省 85 倍，同时达到相干界。**

### 这个设计为什么优雅

它让两种机制各司其职，且**恰好互补**：

- **beam squint → 角度**（实验 B 证明它极擅长此事）
- **时延域 → 距离**（标度律证明这是唯一可行来源）

而这正好解释了前作为什么失败：**它们用 beam squint 同时做两件事，而它只擅长其中一件。**

这句话可以直接作为论文的一句总结。

---

## 6. 恒模单脉冲对：T=2 确实可实现

第 5 节的 oracle 基用了 $\partial_\theta a_m$，它相当于给 $a_m$ 加 $\propto x_n$ 的**幅度锥削**，
恒模 PS 网络实现不了。解法是单脉冲对。

### 6.1 关键事实：FIM 只依赖 span(V)

合并器列非正交时，天线端噪声经合并后协方差为 $\sigma^2 V^HV$，白化后

$$J_{ij}\propto d_i^H\,V(V^HV)^{-1}V^H\,d_j \;=\; d_i^H P_V\, d_j$$

**只取决于 $V$ 张成的子空间，与具体基无关。** 因此问题化简为：
两个**恒模**波束张成的子空间够不够好。

### 6.2 恒模实现

把所有子载波聚焦到同一点 $(r,\theta)$ 只需

$$\phi_n = \frac{f_0 r_n}{c}\ (\text{PS}), \qquad t_n = \frac{r_n}{c}\ (\text{TTD})$$

代入 Luo2024 Eq. (16) 得 $f_m r_n/c - \phi_n - \tilde f_m t_n = 0$ 对所有 $m$ 成立。
于是 $v_m[n]=e^{-j2\pi f_m r_n/c}$ —— **恒模，无幅度锥削，现有硬件直接可实现。**

两个这样的波束指向 $\hat\theta_0 \pm \delta$，一阶展开即张成 $\mathrm{span}\{a,\partial_\theta a\}$。

### 6.3 $\delta$ 的设计窗口

| $\delta$ / 波束宽度 | 损失 | $\mathrm{cond}(V^HV)$ | 差/和通道功率比 | 判定 |
|---|---|---|---|---|
| 0.00 | 1.00× | 6.5×10⁴ | 1.5×10⁻⁵ | 仅数学成立 |
| 0.03 | 1.00× | 653 | 1.5×10⁻³ | 可用 |
| 0.13 | 1.00× | 25.7 | 3.9×10⁻² | 可用 |
| **0.25** | **1.00×** | **6.2** | **1.6×10⁻¹** | **推荐** |
| 0.50 | 1.05× | 1.36 | 0.73 | 可用 |
| 1.00 | 4.29× | 1.32 | 1.32 | 过宽 |

> **重要**：$\delta\to0$ 时损失仍报 1.00×，这是**完美硬件假设下的数值假象**。
> 数学上 $P_V$ 始终良定，但计算它要对趋于奇异的 $V^HV$ 求逆；任何 PS 量化、
> 校准误差或互耦都会摧毁它。**信息论允许任意 $\delta$，硬件才是下界。**
> 论文里必须同时给出损失曲线**和**条件数曲线，否则这条设计规则不可信。

3-dB 波束宽度 $\approx 0.886\lambda/D = 0.398°$，故推荐 $\delta \approx 0.1°$。

### 6.4 鲁棒性（$\delta = 0.1°$）

| 粗角度误差 | 损失 | | 粗距离误差 | 损失 |
|---|---|---|---|---|
| 0.016°（实验 B 中位） | 1.00× | | 20 m | 1.03× |
| 0.10° | 1.00× | | 47 m（实验 B 中位） | 1.07× |
| 0.20° | 1.03× | | 88 m（实验 B 最坏） | 1.11× |
| 0.40° | 1.44× | | | |

**第三个波束毫无帮助**（中心波束、距离偏移波束、两者全加，损失均为 1.00×）——
再次印证 T=2 已饱和。

### 6.5 全区域验证：连距离猜测都不需要

把距离猜测**固定为 30 m**（不做任何距离估计），扫描整个感知区：

| 真实位置 | 损失 |
|---|---|
| (15 m, 0°) | 1.23×（最坏） |
| (30 m, 任意角) | 1.00× |
| (50 m, 55°) | 1.00× |

**最坏 1.23×。** 即 Stage A 只需交付角度，距离维完全不必估计。

---

## 7. 最终获取方案

> **Stage A（1 次探测）**：一次 beam-squint 扫描 → 粗角度（0.016°），**距离输出丢弃**
> **Stage B（2 次探测）**：指向 $\hat\theta_0 \pm 0.1°$ 的恒模单脉冲对，距离猜测固定
> **总计 T = 3，全部恒模 PS+TTD 可实现，全区域损失 ≤ 1.23×**

| 方案 | T | 距离 RMSE @0 dB |
|---|---|---|
| Luo2024 CBS-Low | 2 | ~0.17 m |
| Zhang2026 | 256 | 0.904 m |
| **本方案** | **3** | **~3×10⁻⁴ m** |

---

## 8. 当前实用设计的差距（保留作对照）

`V_squint`（把 beam-squint 图样直接当测量基）表现不佳：T=64 时仍损失 1.6×，
且非单调（T=3 比 T=2 差），因端点交错策略过于粗糙。
两阶段"粗角度 + 单脉冲对"方案明显更优，`V_squint` 保留作对照即可。

---

## 9. 下一步

以上全部是 **CRLB 分析**。还需**端到端仿真**验证真实估计器在只有 3 次探测下确实能工作 ——
这是把边界分析变成可信结果的最后一步。
