# 测量获取：最小探测数与合并器设计

> **状态**：理论推导 + 数值验证完成。代码 `sim/acquisition.py`。
> **结论**：**T = 3 次探测即可达到相干界**，而 Zhang2026 需要 T = N = 256。

---

## 1. 为什么必须先解决这个问题

这是整篇论文**唯一的致命软肋**：

| 方案 | 感知开销 | 每次拿到什么 |
|---|---|---|
| Luo2024 CBS-Low | **2 次波束扫描** | 每子载波一个标量 |
| Zhang2026 | T = N = **256** 次顺序探测（其 Eq. 9–11） | 完整 N 维阵列快拍 |
| 本方案（朴素实现） | T = N = **256** | 同上 |

审稿人一句话即可否掉：*"你需要完整阵列快拍（256 次探测），CBS-Low 只要 2 次，这个对比公平吗？"*

我们之前把这一点作为**攻击 Zhang2026 的 A6**（隐藏 128× 开销）—— 但朴素实现继承了同样的成本，
不解决就是回旋镖。

---

## 2. 含合并器的 CRLB

用 T 个模拟合并向量后，每子载波观测 $z_m = V_m^H a_m \alpha$，参数导数变为 $V_m^H d_{i,m}$：

$$J_{ij} \;\propto\; \sum_m \Re\left\{ d_{i,m}^H\, V_m V_m^H\, d_{j,m}\right\}$$

**因此估计器能学到的关于 $(\theta,r)$ 的一切，都活在三维子空间**

$$\mathcal{S}_m = \mathrm{span}\left\{a_m,\;\frac{\partial a_m}{\partial\theta},\;\frac{\partial a_m}{\partial r}\right\}$$

**之内**。若 $V_m$ 的列张成 $\mathcal{S}_m$，则信息零损失。

注意 $\mathcal{S}_m$ 是**频率相关**的（$a_m$ 随 $f_m$ 旋转）。纯 PS 合并器频率恒定、无法跟踪；
**PS+TTD 可以** —— 这给 TTD 硬件提供了一个**信息论层面的理由**，
而整条 beam-squint 文献只把 TTD 当作"抑制斜视"的工具。

---

## 3. 数值验证：T = 2 即零损失

$N{=}256$，64 个子载波，$r{=}30$ m，$\theta{=}15°$，每阵元 SNR = 0 dB。
全阵列（$T{=}N{=}256$）基准：RMSE$_r \geq 3.044\times10^{-4}$ m。

| T | oracle 子空间 RMSE$_r$ | 损失 | 随机 PS | 损失 |
|---|---|---|---|---|
| 1 | 奇异（无角度信息） | — | 5.49×10⁻³ | 18.0× |
| **2** | **3.044×10⁻⁴** | **1.00×** | 2.88×10⁻³ | 9.5× |
| 3 | 3.044×10⁻⁴ | 1.00× | 2.60×10⁻³ | 8.5× |
| 64 | （T=2 已饱和） | — | 5.94×10⁻⁴ | 1.9× |

且在整个感知区一致成立（$r\in\{15,30,50\}$ m × $\theta\in\{0°,30°,55°\}$，损失恒为 1.000×）。

### 为什么 T 这么小 —— 与标度律直接咬合

$$d_{r,m} \approx -j\frac{2\pi f_m}{c}\,a_m$$

**距离信息本身就沿着 $a$ 方向**，靠的是系数随 $f_m$ 的变化。所以：

> **测距不需要空间自由度。空间维只用来测角度。**

$T{=}1$ 失败正是因为 $\|a_m\|^2$ 与 $\theta$ 无关 → 无角度信息 → FIM 奇异。
$T{=}2$（$\mathrm{span}\{a_m, \partial_\theta a_m\}$）补上角度维后即告饱和。

---

## 4. 关键性质：对角度敏感，对距离免疫

匹配合并器建立在**粗估计** $(\hat r_0, \hat\theta_0)$ 而非真值上时：

| 合并器角度误差 | 损失 | | 合并器距离误差 | 损失 |
|---|---|---|---|---|
| 0.01° | 1.00× | | 1 m | 1.00× |
| 0.10° | 1.00× | | 5 m | 1.00× |
| 0.50° | 2.15× | | 15 m | 1.02× |
| 1.00° | 4.55× | | 40 m | 1.06× |
| 5.00° | 26.1× | | **70 m** | **1.09×** |

**对角度极敏感（需优于 ~0.1°），对距离几乎免疫（70 m 误差只损失 9%）。**

而实验 B 已证明 beam-squint 粗估计**恰好**是：角度中位误差 **0.016°**，距离中位误差 **47 m**。

在实验 B 实际交付的精度下：

| 粗估计误差 | 损失 |
|---|---|
| 0.016° + 0 m | 1.00× |
| 0.016° + 47 m（实验 B 中位） | **1.07×** |
| 0.016° + 88 m（实验 B 最坏） | 1.11× |

---

## 5. 由此确定的获取方案

> **Stage A（1 次探测）**：一次 beam-squint 扫描 → 粗角度，精度 0.016°。
> **距离输出直接丢弃**（实验 B 已证明它是垃圾）。
>
> **Stage B（2 次探测）**：两个匹配到粗角度的合并器，距离随便猜。
>
> **总计 T = 3。**

| 方案 | T | 距离 RMSE @0 dB |
|---|---|---|
| Luo2024 CBS-Low | 2 | ~0.17 m（其 Fig. 14，2D） |
| Zhang2026 | 256 | 0.904 m（本仓库复现） |
| **本方案** | **3** | **~3×10⁻⁴ m** |

**与 CBS-Low 同量级的开销，比 Zhang2026 省 85 倍，同时达到相干界。**

### 这个设计为什么优雅

它让两种机制各司其职，且**恰好互补**：

- **beam squint → 角度**（实验 B 证明它极擅长此事）
- **时延域 → 距离**（标度律证明这是唯一可行来源）

而这正好解释了前作为什么失败：**它们用 beam squint 同时做两件事，而它只擅长其中一件。**

这句话可以直接作为论文的一句总结。

---

## 6. 尚未解决的一点（下一步，必须诚实处理）

上述 $T{=}2$ 的 oracle 结果用的是 $\mathrm{span}\{a_m, \partial_\theta a_m\}$ 的**正交基**。
问题在于：$\partial_\theta a_m$ 相当于 $a_m$ 加了一个 $\propto x_n$ 的**幅度加权**，
而**恒模 PS 网络无法实现幅度锥削**。

即：*信息论说 T=2 够了，但恒模 PS+TTD 硬件能否实现那两个方向，是另一个问题。*

**标准解法**（下一步验证）：用一对指向 $\hat\theta_0 \pm \delta$ 的波束（单脉冲对）
去近似张成 $\{a, \partial_\theta a\}$ —— 这两个波束**是**恒模 PS+TTD 可实现的。
需要验证的是：$\delta$ 取多少能逼近 oracle 界，以及损失多少。

在这一点验证之前，论文里应表述为
"*T=2 is the information-theoretic minimum; we realize it approximately with a
constant-modulus monopulse pair*"，而不是直接声称 T=2 可实现。

---

## 7. 当前实用设计的差距

`V_squint`（把 beam-squint 图样当测量基）目前表现不佳：T=64 时仍损失 1.6×，
且非单调（T=3 比 T=2 差），因为端点交错方式过于粗糙。
这条路暂时**不如**"粗角度 + 匹配对"的两阶段方案，除非重新设计交错策略。
建议以两阶段方案为主线，`V_squint` 作为对照。
