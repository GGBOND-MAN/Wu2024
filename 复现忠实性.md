# 基线复现的忠实性：做到了什么，没做到什么

> 审稿人必问的问题。此处逐条交代，不回避。

---

## 1. 逐方程实现的部分

| 方案 | 实现的方程 | 文件 |
|---|---|---|
| Zhang2026 | Eq. (23)–(25) 粗估计；(30)–(35) 几何补偿平滑；(39)–(41) EVD + 局部 MUSIC；(44) 谱几何平均；±1°/±1 m 搜索窗 | `sim/estimators.py` |
| Luo2024 CBS-Low | Sec. IV-B 两次扫描（角度扫 → 该角度上径向扫） | `sim/baselines.py` |
| Luo2024 CBS-High | Sec. IV-C；Eq. (26) 角度平均；Eq. (27)–(28) 相位测距，**精确求值**而非手推近似 | `sim/baselines.py` |

---

## 2. ✅ 关键验证：复现了 Luo2024 自己报告的数值

在 **Luo2024 Sec. V-C 自己的配置**下（N=128, W=1 GHz, M+1=2048, 用户 (60 m, 20°), SNR=10 dB）：

| | Luo2024 报告 | 本复现（交错 2°） | 本复现（交错 30°） |
|---|---|---|---|
| P=4 角度 | 0.031° | **0.0305°** | 0.0331° |
| P=12 角度 | 0.018° | **0.0188°** | 0.0171° |
| P=4 距离 | 0.165 m | 0.230 m | 0.0299 m |
| P=12 距离 | 0.037 m | 0.230 m | 0.0184 m |

**角度复现到 2–4% 以内** —— 这是复现忠实性的直接证据。
距离同数量级，取决于交错量（见下）。

## 3. ✅ 关键验证：Zhang2026 复现的两条旁证

1. **形状**：用 Fresnel 生成数据时，本复现重现了它 Fig. 13 的单调改善形状（RMSE 降到 2.13×10⁻⁴ °）。
2. **最优性**：本复现跑在正确非相干 CRLB 的 **1.40 倍**上 —— 该算法类别最多再改进 40%，
   "实现得不好"无法解释它与宣称值之间 315 倍的差距。

---

## 4. ⚠️ 论文未指定、由我选定的参数

这是复现的**根本局限**，必须在论文中列出。

| # | 参数 | 论文状态 | 我的取值 | 影响 |
|---|---|---|---|---|
| 1 | Zhang2026 **\|S\|**（精化用的子载波数） | 正文定义了符号，**Table II 中没有数值** | 5 | **直接决定其可达精度**（非相干界 ∝ 1/√\|S\|） |
| 2 | Zhang2026 局部网格分辨率 | 未给出 | 多级缩放 | 影响精度上限与复杂度 |
| 3 | Luo2024 CBS-High **扫描交错量** | **完全未指定** | 30°（调至对其最有利） | **见下，影响巨大** |
| 4 | Luo2024 **r_mid1 / r_mid2** | 只说"select appropriate values such that ξ₂ ≈ 2πp"，**无准则** | (r_min+r_max)/2 | 影响角度阶段的解耦近似 |
| 5 | Luo2024 CBS-High 一维搜索网格 P_R | 未给出 | 1500 | 分辨率下限 |

### 交错量的影响有多大 —— 顺带证实了一条批评

同一实现、同一配置，**仅改交错量**：

| 交错量 | P=4 距离 RMSE |
|---|---|
| 2° | 0.230 m |
| **8°** | **11.14 m** ← 模糊失效 |
| 30° | 0.030 m |

**跨越近三个数量级。** 这正是本工作指出的"Luo2024 从未讨论 Eq. (27) 的 2π 模糊"的实证：
该参数既未指定、又决定成败。论文中应把这一条作为对 CBS-High 的**可复现性**批评，
而非仅仅作为精度批评。

---

## 5. ⚠️ 我刻意做的模型改动

**Luo2024 原本是下行 + 用户反馈；本复现改为单站回波**，以使所有方案面对同一份观测。

- 理由：Zhang2026 是单站模型，若不统一则无法同位置对比。
- 代价：这**不是** Luo2024 的原始设定，论文中必须声明。
- 已同时统一噪声定义（每阵元绝对 SNR），避免按扫描平均功率归一化给基线灌入过量噪声。

---

## 6. ❌ 未实现的部分

| 项 | 原因 |
|---|---|
| Zhang2026 多用户分离细节（Sec. IV-C-2） | 本工作的多用户对比基于 CRLB + 自有估计器 |
| Zhang2026 UPA 扩展（Sec. IV-C-3） | 超出 ULA 对比范围 |
| Luo2024 CBS-2BS（Sec. IV-D） | 需第二个基站，系统假设不同 |
| Zhang2026 的其它基线：深度学习 [21]、DFT 码本 [16]、DCD-MUSIC [22] | 未复现；论文中不应声称与它们对比 |

---

## 7. ❌ 尚未做的验证

- **未**将本复现的 Zhang2026 曲线与其 Fig. 10 的发表数值逐点比对
  （只比对了 Fig. 13 的**形状**）。若要在论文中声称"忠实复现"，这一条值得补。

---

## 8. 复现过程中发现的自身 bug（已修，记录以备复核）

| bug | 症状 | 触发条件 |
|---|---|---|
| 噪声按扫描平均功率归一化 | 给基线灌入过量噪声 | 一直存在，直到改为每阵元 SNR |
| CBS-High 距离搜索只用公共时延项 | 所有估计钉在网格边缘 | 一直存在，直到精确求值 Eq. (28) |
| 搜索网格下界 = r_min | r=r_min 的用户"零误差"（实为失效） | r_min 恰为真值时 |
| 搜索网格下界可达 0 | `dist_fresnel` 除零 → NaN → argmax 返回首格 | **r_min ≤ 5**，如 Luo2024 Fig. 13 的配置 |

前两个若不修，会系统性地低估基线；后两个会产生看似正常实则错误的数值。

---

## 9. 标定结果：Luo2024 的交错量参数化（已实测）

用 Luo2024 Fig. 13 的四个锚点标定，两种参数化对比：

| 参数化 | P=4→P=12 距离改善比 | 能否达到论文的 4.46× |
|---|---|---|
| `total`（P 次扫描均分固定总跨度） | 1.35–2.12 | **否**，上限约 2.1 |
| `step`（相邻扫描固定步长，跨度随 P 增长） | 4.50–6.27 | **是** |

**结论：Luo2024 用的是固定步长，不是固定总跨度。** 前者的跨度随 (P−1) 增长，
峰值子载波的频率散布随之变宽，测距分辨率因而随 P 改善得比 $\sqrt{P}$ 平均快得多。
论文只说各次扫描的跨度"slightly different"，无法区分这两种，而它们的 P 标度完全不同。

### 取值：step = 2.0°，而非脚本按平均失配选出的 1.0°

| step | P=4 角度 | P=12 角度 | P=4 距离 | P=12 距离 | 比值 |
|---|---|---|---|---|---|
| 1.0° | 1.19× | 0.94× | **1.43×** | **1.02×** | 6.27 |
| **2.0°** | **1.06×** | **1.07×** | **0.68×** | **0.68×** | **4.50** |

2.0° 的四个偏差构成**两组干净的常数**（角度一致 1.06–1.07×，距离一致 0.68×），
且 P 标度比 4.50 与论文的 4.46 几乎相同。1.0° 的偏差**不一致**（1.43× vs 1.02×），
只是平均值恰好更小。

**一致的比例因子可以解释**（SNR 定义约定、网格分辨率），**不一致的不能**。
因此 2.0° 才是对机制的正确辨识 —— 它复现了论文的*形状*，只差一个全局标度。

距离偏 0.68× 意味着本复现**比论文报告值好 1.5 倍**，这对基线是安全方向：
不存在低估基线、从而高估本方案优势的风险。

标定脚本的选择指标已改为 **失配 + 不一致度**，避免再次选中形状错误但平均值小的点。

### 上限：step ≥ 8° 失效

step=8° 时 P=12 的跨度达 11×8 = 88°，波束指向超出 ±90°，角度 RMSE 跳到 0.405°、
比值反转为 0.27。这是该参数化的物理上限，标定时不应越过。

---

## 10. 原作者回复（Luo2024）——关键佐证与仍存的不确定性

联系 Luo2024 作者后得到的答复。原始工程完成于 2022 年，代码已无法找回，
以下为作者能够确认的定性信息。

### ✅ 证实了 `step` 参数化

> "CBS-High 的不同次 Beam Sweeping 需要采用略有差异、且均能覆盖目标感知范围的角度区间。
> 我印象中当时采用了关于 0 度对称的区间，即 theta_max,p = −theta_min,p，
> 但各次区间的具体端点目前无法确认，**应该是 60，61，62 …… 这样排列上去**，
> 或者是 60.1，60.2，60.3 这样排上去，或者是比 0.1 更大一些的角度间隔。"

"60, 61, 62 …" 即各次扫描的半跨度**逐次增长固定步长**，总跨度随 (P−1) 增长 ——
**正是本工作从 P 标度反推出的 `step` 参数化**，`total`（固定总跨度）被排除。

这条独立佐证很重要：机制是从论文自己的 P=4→P=12 改善比（4.46×）反推出来的，
而 `total` 模式的上限只有 2.1×。

### 取值改判为 step = 1.0°

作者称步长约 1°。据此修正此前基于一致性判据选出的 2.0°：

| step | 平均失配 | 不一致度 | P4→P12 | P=12 距离 vs 锚点 |
|---|---|---|---|---|
| **1.0°（采用）** | **0.149** | 0.567 | 6.27 | **1.02×** |
| 2.0° | 0.224 | **0.021** | 4.50 | 0.68× |

**作者的独立佐证优先于本工作自定的启发式指标**，且 1.0° 将 P=12 锚点对到 1.02×。

6.27 这个比值也能解释：跨度比 (11/3)=3.67 乘以 √P 平均的 √3，得 6.35，与实测吻合。
论文报告的 4.46 慢于此，很可能是其一维搜索网格在 P=12 时成为瓶颈 ——
作者亦称 P_R "目前无法准确核实"。

### ✅ 证实了 r_mid1 = r_mid2

> "r_mid1 和 r_mid2 不一定必须相等；但我印象中，当时部分角度感知仿真采用了二者相等的设置。"

与本复现的假设一致。而实测表明该参数在 0.15–0.85 区间内对角度锚点无影响
（变异 10.3% ≈ 60 次试验的理论标准误差 9.1%），故取区域中点即可。

### ✅ 独立证实了本工作的距离标度律

> "随着用户距离增大，距离维的聚焦和分辨能力会逐渐下降，
> 因此较近用户通常能够获得更好的距离估计性能。"

这正是本工作推导的 $\mathrm{RMSE}_r^{\rm curv} \propto r^2$ 的机制，
由基线论文作者本人从方法原理出发独立确认。

### ✅ 认可了本工作的对比方法论

> "不同学者和公司在仿真实验中，对 SNR、SINR、IoT 这些条件的绝对设置都不尽相同，
> 所以当你完成对 A 算法原理的实现后，再进行 B 算法的研究时，
> **只要保证 A 和 B 算法在相同的、公平的条件下进行性能对比即可**。"

本工作正是如此处理：同一用户位置、同一噪声、同一每阵元 SNR 定义。
这也印证了此前把噪声归一化从"扫描平均功率"改为"每阵元绝对 SNR"的必要性。

### ⚠️ 仍无法确认的

| 项 | 作者说明 |
|---|---|
| 交错步长的确切值 | 1° / 0.1° / 或稍大于 0.1°，无法确认 |
| r_mid1, r_mid2 的具体数值 | 无法确认 |
| 一维搜索步长 / P_R | 无法确认 |
| 每幅图的蒙特卡洛次数 T | 回忆为"几万次量级"，具体无法确认 |

**蒙特卡洛次数值得注意**：作者称当年用"几万次量级"，而本标定用的是 60 次。
最终对比图应提高到至少数千次。
