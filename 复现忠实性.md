# 基线复现的忠实性：做到了什么，没做到什么

> 审稿人必问的问题。此处逐条交代，不回避。

---

## 1. 逐方程实现的部分

| 方案 | 实现的方程 | 文件 |
|---|---|---|
| Zhang2026 | Eq. (23)–(25) 粗估计；(30)–(35) 几何补偿平滑；(39)–(41) EVD + 局部 MUSIC；(44) 谱几何平均；±1°/±1 m 搜索窗 | `sim/estimators.py` |
| Luo2024 CBS-Low | Sec. IV-B 两次扫描（角度扫 → 该角度上径向扫） | `sim/baselines.py` |
| Luo2024 CBS-High | Sec. IV-C；Eq. (26) 角度平均；Eq. (27)–(28) 相位测距，**精确求值**而非手推近似 | `sim/baselines.py` |

---

## 2. ✅ 关键验证：复现了 Luo2024 自己报告的数值

在 **Luo2024 Sec. V-C 自己的配置**下（N=128, W=1 GHz, M+1=2048, 用户 (60 m, 20°), SNR=10 dB）：

| | Luo2024 报告 | 本复现（交错 2°） | 本复现（交错 30°） |
|---|---|---|---|
| P=4 角度 | 0.031° | **0.0305°** | 0.0331° |
| P=12 角度 | 0.018° | **0.0188°** | 0.0171° |
| P=4 距离 | 0.165 m | 0.230 m | 0.0299 m |
| P=12 距离 | 0.037 m | 0.230 m | 0.0184 m |

**角度复现到 2–4% 以内** —— 这是复现忠实性的直接证据。
距离同数量级，取决于交错量（见下）。

## 3. ✅ 关键验证：Zhang2026 复现的两条旁证

1. **形状**：用 Fresnel 生成数据时，本复现重现了它 Fig. 13 的单调改善形状（RMSE 降到 2.13×10⁻⁴ °）。
2. **最优性**：本复现跑在正确非相干 CRLB 的 **1.40 倍**上 —— 该算法类别最多再改进 40%，
   "实现得不好"无法解释它与宣称值之间 315 倍的差距。

---

## 4. ⚠️ 论文未指定、由我选定的参数

这是复现的**根本局限**，必须在论文中列出。

| # | 参数 | 论文状态 | 我的取值 | 影响 |
|---|---|---|---|---|
| 1 | Zhang2026 **\|S\|**（精化用的子载波数） | 正文定义了符号，**Table II 中没有数值** | 5 | **直接决定其可达精度**（非相干界 ∝ 1/√\|S\|） |
| 2 | Zhang2026 局部网格分辨率 | 未给出 | 多级缩放 | 影响精度上限与复杂度 |
| 3 | Luo2024 CBS-High **扫描交错量** | **完全未指定** | 30°（调至对其最有利） | **见下，影响巨大** |
| 4 | Luo2024 **r_mid1 / r_mid2** | 只说"select appropriate values such that ξ₂ ≈ 2πp"，**无准则** | (r_min+r_max)/2 | 影响角度阶段的解耦近似 |
| 5 | Luo2024 CBS-High 一维搜索网格 P_R | 未给出 | 1500 | 分辨率下限 |

### 交错量的影响有多大 —— 顺带证实了一条批评

同一实现、同一配置，**仅改交错量**：

| 交错量 | P=4 距离 RMSE |
|---|---|
| 2° | 0.230 m |
| **8°** | **11.14 m** ← 模糊失效 |
| 30° | 0.030 m |

**跨越近三个数量级。** 这正是本工作指出的"Luo2024 从未讨论 Eq. (27) 的 2π 模糊"的实证：
该参数既未指定、又决定成败。论文中应把这一条作为对 CBS-High 的**可复现性**批评，
而非仅仅作为精度批评。

---

## 5. ⚠️ 我刻意做的模型改动

**Luo2024 原本是下行 + 用户反馈；本复现改为单站回波**，以使所有方案面对同一份观测。

- 理由：Zhang2026 是单站模型，若不统一则无法同位置对比。
- 代价：这**不是** Luo2024 的原始设定，论文中必须声明。
- 已同时统一噪声定义（每阵元绝对 SNR），避免按扫描平均功率归一化给基线灌入过量噪声。

---

## 6. ❌ 未实现的部分

| 项 | 原因 |
|---|---|
| Zhang2026 多用户分离细节（Sec. IV-C-2） | 本工作的多用户对比基于 CRLB + 自有估计器 |
| Zhang2026 UPA 扩展（Sec. IV-C-3） | 超出 ULA 对比范围 |
| Luo2024 CBS-2BS（Sec. IV-D） | 需第二个基站，系统假设不同 |
| Zhang2026 的其它基线：深度学习 [21]、DFT 码本 [16]、DCD-MUSIC [22] | 未复现；论文中不应声称与它们对比 |

---

## 7. ❌ 尚未做的验证

- **未**将本复现的 Zhang2026 曲线与其 Fig. 10 的发表数值逐点比对
  （只比对了 Fig. 13 的**形状**）。若要在论文中声称"忠实复现"，这一条值得补。

---

## 8. 复现过程中发现的自身 bug（已修，记录以备复核）

| bug | 症状 | 触发条件 |
|---|---|---|
| 噪声按扫描平均功率归一化 | 给基线灌入过量噪声 | 一直存在，直到改为每阵元 SNR |
| CBS-High 距离搜索只用公共时延项 | 所有估计钉在网格边缘 | 一直存在，直到精确求值 Eq. (28) |
| 搜索网格下界 = r_min | r=r_min 的用户"零误差"（实为失效） | r_min 恰为真值时 |
| 搜索网格下界可达 0 | `dist_fresnel` 除零 → NaN → argmax 返回首格 | **r_min ≤ 5**，如 Luo2024 Fig. 13 的配置 |

前两个若不修，会系统性地低估基线；后两个会产生看似正常实则错误的数值。
