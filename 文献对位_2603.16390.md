# 精读 arXiv:2603.16390 —— 与本工作最接近的一篇

> *Near-Field Wideband Localization using TTD-Based Terahertz Extremely Large-Scale Arrays*
> 同属 TTD + 近场 + 宽带 + 定位，是 related work 里必须精确对位的一篇。
> 此前只读摘要，现已读全文。

---

## 1. 确认的部分（对本工作有利）

| 项 | 该文的做法 | 出处 |
|---|---|---|
| 距离信息来源 | **curvature-of-arrival（波前曲率）** | 其贡献条目原文 |
| TTD 的用途 | **补偿 beam-split**，不是测量时延 | 全文一致 |
| ToA / 时延测距 | **完全没有** | 全文无相关表述 |
| FIM / PEB 的基础 | **仅曲率**。Eq. (29)–(32) 的 FIM 来自导向矩阵对 $(d_k,\theta_k)$ 的梯度，**无时延项** | — |
| 子载波数 | **M = 12**（其 Table I） | — |

> **最关键的一条**：它的信号相位**确实依赖 $f_m$**（其 Eq. 3: $v_{m,k,n} = 2\pi f_m d_{k,n}/c$），
> 但它把这个频率依赖当作**需要被 TTD 补偿的 beam-split 麻烦**，而不是当作**测距的信息源**。
>
> 这恰恰是本工作论点的最强佐证：**整条脉络握着时延，却把它当作要消除的干扰。**

且它只用 **12 个子载波** —— 即便想做时延测距，分辨率也无从谈起。

---

## 2. 我此前表述有误，必须修正

### ❌ 修正一：它用的是**精确球面波**，不是 Fresnel

其 Eq. (12): $d_{k,n} = \sqrt{r_n^2 + d_k^2 - 2 r_n d_k \cos\theta_k}$ —— **完整非线性近场模型**。

**因此实验 A 的攻击（Fresnel 截断 / inverse crime）对该文不成立。**
那条攻击只适用于 Zhang2026 和 arXiv:2511.23187（后者明确用二阶近似）。
论文里不能笼统说"整条脉络都用 Fresnel"。

### ⚠️ 修正二：它**已经**在做"用 FIM 优化模拟合并系数"

其 Algorithm 1 通过**最大化 Fisher 信息**来优化模拟系数，Algorithm 2 迭代联合优化波束聚焦与定位。

这与本工作的"最小探测数 / 合并器设计"在**思路上有重叠**，必须承认。

**但区别是决定性的，而且这个区别比原来的表述更锋利**：

> 它最大化的 FIM **本身就缺了主导项**。
> 我们的贡献不是"优化合并器"（它已经做了），而是
> **"它所优化的那个 FIM 里没有时延项"** ——
> 在 $r = 30$ m 处，被漏掉的那一项比它优化的那一项大 **41,000 倍**。
>
> 优化一个残缺目标函数，优化得再好也到不了正确的界。

---

## 3. 修正后的 related work 定位

| 论文 | 距离来源 | 近场模型 | 合并器设计 | 与本工作的关系 |
|---|---|---|---|---|
| Luo2024 CBS-Low | 曲率 | Fresnel | 手选 | 对照 |
| Luo2024 CBS-High | **稀疏时延**（P≈10 个不规则频点） | Fresnel | 手选 | **已在正确方向上，但只采样了 P 个点** |
| Zhang2026 | 曲率 + 非相干融合 | Fresnel | 无 | 主要攻击对象；相对 CBS-High **倒退** |
| arXiv 2511.23187 | 曲率 | **二阶近似** | 无（原子范数） | 对照 |
| **arXiv 2603.16390** | **曲率** | **精确球面波** | **FIM 最大化** | **最接近；但其 FIM 缺时延项** |
| **本工作** | **全频带相干时延** | **精确球面波** | **FIM 最小 T = 2 + 恒模实现** | — |

---

## 4. Introduction 该怎么写（修订）

原来的写法"整条脉络都停留在 Fresnel 近似的曲率上"**不准确**，2603.16390 用的是精确模型。
准确的写法应当只锁定**距离信息来源**这一个维度：

> Across the beam-squint and TTD line of near-field localization -- from the
> controllable-squint schemes of [Luo2024] through [Zhang2026] to the
> Fisher-optimal analog design of [2603.16390] -- range is recovered from the
> curvature of the spherical wavefront. The frequency dependence of the received
> phase is treated throughout as beam split, an impairment for the true-time
> delays to compensate. Yet that same dependence is an exact, approximation-free
> measurement of propagation delay, and at the operating ranges these systems
> declare it carries the range information that curvature has already lost: the
> two are equal only within about 15 cm of the array.

这个表述的好处：
- 不依赖"它们都用 Fresnel"这个**不成立**的前提
- 把攻击锁在**唯一真正共同**的那一点上：距离来自曲率
- 把 TTD 的角色反转（它们用来补偿，我们用来测量）说成一句话
