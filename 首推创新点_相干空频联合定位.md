# 首推创新点：相干空-频（时延域）近场联合定位

> 一句话：**两篇论文都握着 3 GHz 带宽，却都只做了非相干的窄带处理。距离信息的主矿脉——频率轴上的相位斜率——被完整地丢掉了。**

---

## 1. 核心观察（这是整个创新点的全部数学基础）

天线 $n$、子载波 $m$ 上的接收相位，**不做任何近似**，就是

$$\Phi(n,m) \;=\; -\frac{2\pi f_m}{c}\, r_n(\theta, r), \qquad f_m = f_0 + m\,\Delta f$$

把 $f_m$ 展开：

$$\boxed{\;\Phi(n,m) \;=\; \underbrace{-\frac{2\pi f_0}{c} r_n}_{\text{与 }m\text{ 无关}} \;\underbrace{-\;\frac{2\pi \Delta f}{c}\, m\, r_n}_{\text{关于 }m\text{ 严格线性}}\;}$$

**沿频率轴，固定天线 $n$，相位关于子载波索引 $m$ 是精确线性的，斜率正比于 $r_n$。**

这一步的关键性质：

| 性质 | 说明 |
|---|---|
| **零近似** | 不需要 Fresnel 展开、不需要远场平面波、不需要傍轴近似。这只是传播时延的定义。 |
| **对阵列几何无要求** | ULA、UPA、任意共形阵、甚至阵元位置未标定，公式都成立。 |
| **每根天线独立成立** | $N$ 根天线给出 $N$ 个**独立**的距离观测。 |

对比：Zhang2026 的整个 MUSIC 建立在 Fresnel 二次相位项 $\frac{n^2d^2\cos^2\theta}{2r}$ 上——它自己在 Eq. (17)(18) 证明了这一项在大 $N$ / 小 $r$ 时失效（我的实验 A 实测 $N{=}1024$ 时残余相位达 106°）。**上式完全不依赖那一项。**

---

## 2. 由此得到的估计器（三步，全程无网格）

### Step 1｜逐天线时延测距
对每根天线，从 $Y[n,:]$ 沿频率轴的相位斜率读出 $r_n$：
- 粗：沿 $m$ 做 FFT，峰值 bin 给出 $r_n$，原生分辨率 $c/(2W)$ = **5 cm**（$W$=3 GHz）
- 精：按粗值去旋转后残余相位不超过 ±0.39 rad，直接最小二乘拟合斜率

> ⚠️ **符号约定陷阱**（我调试时踩过）：必须用 `fft(Y.conj())` 而非 `ifft`，否则粗估计会落到错误的混叠周期上，产生恰好 $c/\Delta f = 204.66$ m 的常数偏置。

**实测（$N{=}256$, $M{=}2048$, $W{=}3$ GHz，精确球面波数据）：**

| 每阵元 SNR | 逐天线测距偏置 | 逐天线测距 std |
|---|---|---|
| 0 dB | −1.5×10⁻⁴ m | **1.1 mm** |
| 20 dB | −1.1×10⁻⁵ m | 83 μm |
| 40 dB | −5.3×10⁻⁷ m | 8.6 μm |

**单根天线、0 dB、不做任何阵列处理，就有毫米级测距。** 这就是被丢掉的那座矿。

### Step 2｜精确几何多点定位
把 $N$ 根天线当作 $N$ 个**虚拟锚点**，在**精确球面几何**上做非线性最小二乘：

$$(\hat\theta, \hat r) \;=\; \arg\min_{\theta, r} \sum_{n} \Big| \hat r_n - \sqrt{r^2 + x_n^2 - 2 r x_n \sin\theta} \Big|^2$$

**近场定位从"病态的曲率拟合问题"变成了"良态的多点定位问题"。** 这是整个创新点的概念内核。

### Step 3｜局部 ML 精化（可选）
Step 1–2 的距离已经贴着 CRLB，但角度还差一截（只用了 $\hat r_n$，丢掉了残余相位）。在精确流形上做一次相干匹配滤波的局部极大化：

$$\max_{\theta,r}\; \Big| \sum_{n,m} a^*(\theta,r,f_m)_n \, Y[n,m] \Big|^2$$

起点已经精确到 ~0.02° / ~0.1 mm，这是一个**光滑的二参数局部搜索，几十次函数求值**即可。
> 对比 Zhang2026 Sec. IV-C-1 为同样的标称分辨率需要 **4,000,000 点网格**。

---

## 3. 为什么这是"正确的"方向：CRLB 说了算

在 $r{=}30$ m, $\theta{=}15°$, $N{=}256$, $W{=}3$ GHz, $M{=}2048$, 每阵元 SNR = 0 dB：

| 界 | RMSE$_\theta$ 下界 | RMSE$_r$ 下界 | 说明 |
|---|---|---|---|
| Zhang2026 Appendix B | 1.13×10⁻² ° | **3.51×10⁻⁵ m** | ❌ 单频点 + 未边缘化 $\alpha$，**不是它自己问题的界** |
| 正确窄带（边缘化 $\alpha$） | 1.13×10⁻² ° | **2.22 m** | 曲率是唯一距离信息源时的真实极限 |
| 宽带**非相干**（逐子载波 $\alpha_m$） | 2.50×10⁻⁴ ° | **4.92×10⁻² m** | ⭐ **Zhang2026 算法类的真实极限** |
| 宽带**相干**（共享 $\alpha$，时延可观测） | 2.50×10⁻⁴ ° | **5.38×10⁻⁵ m** | ⭐ **本创新点可达的极限** |

三个结论，每一个都很重要：

1. **Zhang2026 的 CRLB 错了 63,281 倍。** 它在 Appendix B 声明 $s_l$ 是未知确定性参数，却从未做 Schur 补边缘化。$\partial\Phi/\partial r$ 的主导项 $-2\pi f/c$ 是沿阵列的**常数**，完全被未知复幅度 $\alpha$ 的相位吸收——它却把这部分信息算给了估计器。（角度界不受影响，因为 $\partial\Phi/\partial\theta \propto x\cos\theta$ 关于阵列中心是奇函数，本就与 $a$ 近似正交。误差恰好精准地落在它宣称毫米精度的那一维上。）

2. **它宣称的 0.001 m 对它自己的算法在信息论上不可能。** 逐子载波 MUSIC + 几何平均融合（Eq. 44）是严格非相干的。宣称值低于非相干界这一结论，在**两种 SNR 定义、Fig. 10 的每一个 SNR 点上都成立**：

   | Fig. 10 的 SNR 标注 | 若为每阵元 SNR | 若为阵列级 SNR（每阵元 −24.1 dB） |
   |---|---|---|
   | 0 dB | 界 4.91×10⁻² m，宣称值低 **49×** | 界 7.86×10⁻¹ m，宣称值低 **786×** |
   | 10 dB | 界 1.55×10⁻² m，宣称值低 **16×** | 界 2.49×10⁻¹ m，宣称值低 **249×** |
   | 20 dB（其横轴上限） | 界 4.91×10⁻³ m，宣称值低 **4.9×** | 界 7.86×10⁻² m，宣称值低 **79×** |

   论文从未说明 SNR 是阵元级还是阵列级——但**两种读法都推不出它的结论**，所以这个攻击不依赖于该歧义的解决方式。

3. **但 0.001 m 是真实可达的——只是需要相干处理。** 相干界是 5.38×10⁻⁵ m。**非相干与相干之间隔着 913 倍，这就是留给这个创新点的全部空间。**

---

## 4. 原型实测：确实达到了界

$N{=}256$，$M{=}2048$，精确球面波数据，全程无网格：

**用户 (30 m, 15°)**

| 每阵元 SNR | RMSE$_r$ (Step 1–2) | 相干 CRLB$_r$ | 效率比 |
|---|---|---|---|
| −10 dB | 2.24×10⁻⁴ m | 1.70×10⁻⁴ m | 1.32× |
| 0 dB | **7.23×10⁻⁵ m** | 5.38×10⁻⁵ m | 1.34× |
| 10 dB | 1.82×10⁻⁵ m | 1.70×10⁻⁵ m | 1.07× |
| 20 dB | 4.26×10⁻⁶ m | 5.38×10⁻⁶ m | 0.79× |

**用户 (50 m, 50°)** 结果几乎相同（0 dB: 6.83×10⁻⁵ m vs 界 5.38×10⁻⁵ m）——**不像 Zhang2026 那样随距离和角度显著退化**，因为时延测距的精度与 $r$、$\theta$ 基本无关，而曲率测距 $\propto r^2$ 迅速恶化。这本身就是一张很有说服力的对比图。

单次估计耗时约 120 ms（未优化的 numpy，复杂度 $O(NM\log M)$）。

---

## 5. 关键设计约束（必须写进论文，也是审稿人会问的）

**距离模糊。** 相位斜率保持在 $(-\pi,\pi]$ 要求

$$r_n < \frac{c}{2\Delta f} = \frac{cM}{2W}$$

$M{=}2048$, $W{=}3$ GHz $\Rightarrow$ **102.4 m**，覆盖它们 15–50 m 的感知区。
> 我在原型里抽取子载波（$\Delta f$ 变 8 倍）时模糊距离掉到 12.8 m，$r{=}30$ m 直接混叠失败——**子载波抽取会按比例缩小无模糊距离**，这是一条必须写明的设计准则。

**时钟偏移。** 单站回波（Zhang2026 的模型）时延基准已知，$r_n$ 绝对可测。若是下行+用户反馈（Luo2024 的模型），用户时钟偏移 $\tau_{\rm off}$ 使所有 $r_n$ 同时抬升 $c\tau_{\rm off}$ ——但**差分时延正是几何所需**，只需在 Step 2 多加一个待估参数（3 个未知量 $\theta, r, \tau_{\rm off}$，$N$ 个观测），依然良态。这本身就是一个可写的扩展贡献。

**测量获取。** 需要空-频矩阵 $Y \in \mathbb{C}^{N\times M}$。单 RF 链下：
- 朴素做法 = Zhang2026 的 $T{=}N{=}256$ 次顺序探测（我在攻击点 A6 里批评的那 128× 开销）
- **更好的做法**：$T \ll N$ 次随机/Hadamard 模拟合并 + 压缩重建。这正好把攻击点 A6 转成本方案的正面贡献。
- 注意 OFDM 一个符号同时给出全部 $M$ 个子载波，所以**频率维是免费的**——这恰恰是它被浪费得最可惜的原因。

---

## 6. 相对两篇论文的定位（论文 Introduction 可以直接这么写）

| | Luo2024 CBS-High | Zhang2026 JAD-MUSIC | **本方案** |
|---|---|---|---|
| 距离信息来源 | 少数峰值子载波的相位差 | 近场曲率（二次相位项） | **全频带相干时延** |
| 频率维利用 | 部分（$P$ 个峰值子载波） | **无**（逐子载波 + 几何平均） | **完全相干** |
| 近场模型 | Fresnel 二阶 | Fresnel 二阶 | **精确球面波** |
| 阶段结构 | 2 阶段（角→距） | 2 阶段（粗→精） | **无级联**（Step 2/3 是精化不是解模糊） |
| 误差传播 | Δθ→Δr，被放大 $2r\tan\theta$ | 粗估计→协方差结构失真 | **无** |
| 搜索 | 1-D 距离网格 | 2-D 网格，4×10⁶ 点 | **无网格**（+ 可选二参数局部 ML） |
| 依赖轨迹覆盖 | 是（故用 2 次扫描） | 是（**但只用 1 次**，见攻击点 A2） | **否** |
| 距离 RMSE @0 dB | ~0.17 m（2D，其 Fig. 14） | 宣称 0.001 m（界为 0.049 m，不可能） | **7.2×10⁻⁵ m（实测，贴界）** |

**Introduction 的叙事线**：

> 现有基于波束斜视的近场定位工作把带宽**仅仅**当作一个空间扫描机制——用 TTD 把不同子载波指向不同位置，然后从功率峰值读出位置。这确实把斜视从缺陷变成了资产，但只用到了带宽的"**指向**"价值，没有用到它的"**测距**"价值。本文指出：在同一批 OFDM 观测中，频率轴上的相位斜率直接给出每根天线到目标的绝对传播距离，且这一关系**不含任何近场近似**。据此，近场定位可以重构为精确几何下的多点定位问题，从根本上绕开曲率拟合的病态性、Fresnel 模型失配、以及级联估计的误差传播。

**这个 framing 的好处**：不是"我的算法比你的好"（容易被审稿人当作增量工作），而是"这个问题一直被建模错了"（概念性贡献）。

---

## 7. 建议的论文骨架

1. **Intro**：斜视被用作扫描机制，而非测距机制 → 问题重构
2. **动机实验**：正确 CRLB 的四行表（第 3 节）——非相干界 vs 相干界，913× 差距
3. **理论**：精确线性相位关系 + 无模糊条件 + 时钟偏移扩展 + 可辨识性
4. **算法**：Step 1–3，复杂度 $O(NM\log M)$
5. **测量设计**：$T \ll N$ 压缩模拟合并（把单 RF 链开销从 256 降到十几）
6. **仿真**：
   - 贴合 CRLB（本文档第 4 节）
   - 精确球面波下对 Zhang2026 的复现（**实验 A**，展示其误差地板）
   - 偏离轨迹的鲁棒性（**实验 B**，展示其崩溃而本方案不受影响）
   - 复杂度实测对照（**实验 D**）
   - 多径 / 未知信源数扩展
7. **可选加分**：TTD 硬件量化受限下的性能

---

## 7.5 Step 3 的实测效果（已补跑）

用户 $(-46.634°, 29.974\,\text{m})$，40 次蒙特卡洛，与实验 C 同条件：

| 每阵元 SNR | 角度 Step 1–2 | 角度 Step 1–3 | 改善 | 距离 Step 1–2 | 组合方案 |
|---|---|---|---|---|---|
| −10 dB | 9.58×10⁻² ° | 6.33×10⁻³ ° | 15.1× | 2.49×10⁻⁴ m | 6.33×10⁻³ ° / 2.49×10⁻⁴ m |
| 0 dB | 3.63×10⁻² ° | 2.11×10⁻³ ° | 17.2× | 8.02×10⁻⁵ m | 2.11×10⁻³ ° / 8.02×10⁻⁵ m |
| +10 dB | 7.47×10⁻³ ° | 6.65×10⁻⁴ ° | 11.2× | 2.01×10⁻⁵ m | 6.65×10⁻⁴ ° / 2.01×10⁻⁵ m |
| +20 dB | 2.50×10⁻³ ° | 1.66×10⁻⁴ ° | 15.1× | 5.67×10⁻⁶ m | 1.66×10⁻⁴ ° / 5.67×10⁻⁶ m |

ML 精化把角度效率从 **64–109× CRLB 拉到约 6× CRLB**。注意 Step 1–3 的**距离**反而比
Step 1–2 差 4–5 倍——原因是精化只用了 64 个子载波，而 Step 1–2 用满 2048 个。

**精化阶段的频带用量直接决定最终效率**（SNR = 0 dB，实测）：

| `n_sub`（精化所用子载波数） | RMSE$_\theta$ / CRLB | RMSE$_r$ / CRLB | 耗时 |
|---|---|---|---|
| 64 | 5.15× | 5.07× | 297 ms |
| 256 | 2.78× | 3.40× | 821 ms |
| **1024（半个频带）** | **1.61×** | **1.21×** | 3117 ms |

**结论：用满频带后，角度和距离同时达到相干 CRLB 的 1.2–1.6 倍。** 先前观察到的角度
差距完全来自为提速而抽取子载波，不是方法的固有限制。论文中应直接给出 `n_sub` 与
效率的这条折衷曲线；工程上把 Nelder-Mead 换成 Gauss-Newton 可大幅降低那 3.1 s。

---

## 8. 已知的诚实短板（自己先想清楚，别让审稿人先问）

1. ~~Step 1–2 的角度没到界~~ —— **已解决**（见 7.5）。精化用满频带后角度 1.61×、距离 1.21× CRLB。剩余的是纯工程问题：需用 Gauss-Newton 替代 Nelder-Mead 降低精化耗时。
2. **需要相干宽带观测**，对相噪、CFO、以及采集期间的信道静止性有要求——比逐子载波方案更严格。必须给出相位噪声敏感性分析。
3. **单径 LoS 假设仍在**。多径下每条路径贡献自己的时延峰——这其实是**优势**（时延域天然可分离多径，比角度域好分），但要正式处理信源数估计。
4. **幅度平坦假设** $\alpha_m \approx \alpha$。真实路损 $\propto 1/f_m$，需要在导频已知时先均衡；应给出残余幅度起伏的影响分析。
