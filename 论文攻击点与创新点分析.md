# 两篇 Beam-Squint 近场定位论文的批判性分析与可攻击点

- **Luo2024**：H. Luo, F. Gao, W. Yuan, S. Zhang, "Beam Squint Assisted User Localization in Near-Field ISAC Systems," *IEEE TWC*, vol. 23, no. 5, pp. 4504–4517, May 2024.
- **Zhang2026**：A. Zhang, W. Zhang, C. Zhang, T. Zhang, "Beam Squint Assisted Joint Angle-Distance Localization for Near-Field Communications," *IEEE TVT*, DOI 10.1109/TVT.2026.3706538.

---

## 0. 两篇的定位与真实关系

| | Luo2024 (CBS) | Zhang2026 (JAD-MUSIC) |
|---|---|---|
| 系统 | 下行 + 用户反馈（协作式） | 单基站单站回波（monostatic radar） |
| 硬件 | 单 RF 链，每天线 1 PS + 1 TTD | 同左 |
| 核心 | 用 TTD 控制 squint 轨迹，峰值子载波 → 位置 | 同左（Eq.19/20 与 Luo 的 Eq.19/20 完全相同，仅换符号） |
| 方案 | CBS-Low（2 次扫）/ CBS-High（P 次扫 + 多载波相位差测距）/ CBS-2BS | 粗（功率峰）→ 精（局部近场 MUSIC + 几何补偿空间平滑 + 谱几何平均） |
| 声称精度 | CBS-High: RMSE_θ≈0.018°, RMSE_r≈0.037 m (P=12) | RMSE_θ<0.001°, RMSE_r<0.001 m |

**关键事实**：Zhang2026 的第 III 节（可控 squint 轨迹）**没有任何新推导**，它自己写了"we will briefly review the controllable beam squint control method proposed in [20]"。真正的新东西只有第 IV 节的两阶段估计器。所以它的"贡献 1"（beam pattern design）实质是复述 Luo。

---

## 1. 可以直接攻击 Zhang2026 的地方（按杀伤力排序）

### ★★★★★ A1. 它并没有消除误差传播，只是把误差传播搬到了更危险的地方

这是最致命的一击，因为它直接推翻论文的**唯一卖点**。

Zhang2026 的相位对齐矩阵（Eq. 33）：

```
D_{p,m}(η̂⁽⁰⁾) = diag{ exp(-j2πf_m/c · [r_{n_{p0},q}(η̂⁽⁰⁾) - r_{n_p,q}(η̂⁽⁰⁾)]) }
```

**这个矩阵是用第一阶段的粗估计 η̂⁽⁰⁾ 构造的。** 也就是说：

- Luo 的误差传播：Δθ → Δr（Eq. 21，一阶线性、可解析、可补偿）
- Zhang 的误差传播：Δη⁽⁰⁾ → **协方差矩阵结构失真** → 噪声子空间偏移 → MUSIC 谱峰有偏

第二种**更糟**：它是非线性的、不可解析补偿的，而且会污染子空间正交性——MUSIC 的超分辨完全建立在正交性上。论文对此只有一句空话："the residual phase-alignment error remains within the perturbation bound"，**没有给出任何定量的 perturbation bound，没有给出粗估计误差的容限阈值**。

> **你的切入点**：推导 MUSIC 谱峰偏移量关于 Δη⁽⁰⁾ 的一阶/二阶展开，给出临界阈值 Δη_crit，证明超过阈值后子阵导向矢量对齐失效、信号子空间从秩 1 变成秩 >1（此时 Eq. 40 用"最大特征值对应信号子空间"就错了）。然后提出**自校准迭代对齐**或**免对齐（alignment-free）**方案。这是"打在七寸上"的攻击。

---

### ★★★★★ A2. 粗估计阶段在数学上是欠定的——1 维轨迹无法覆盖 2 维区域

这是**结构性缺陷**，我认为是最容易做成一篇新论文的攻击点。

Beam-squint 轨迹 (θ̃_m, r̃_m) 是 (θ, r) 平面上的**一条一维曲线**。粗估计只用一个标量观测（峰值子载波索引 m\*）去反解**两个未知量** (θ, r)。这在一般情况下不可能。

- 它的 Eq. 25 `(θ̂⁽⁰⁾, r̂⁽⁰⁾) = (θ̃_{m*}, r̃_{m*})` 隐含假设：**用户就落在设计好的那条轨迹上**。
- 论文 Fig. 5：轨迹从 (−60°, 15 m) 到 (60°, 50 m)，UE 在 (15°, 30 m)——恰好在曲线附近。
- **反例**：同样的轨迹，若用户在 (15°, 45 m)，轨迹上 θ=15° 处对应 r ≈ 37 m，粗距离估计误差 ≈ 8 m，**远超它设定的 MUSIC 搜索窗 Δr = 1 m**。第二阶段直接崩溃，输出被窗口硬性截断。

对比：Luo2024 之所以用两次扫描（先角度扫、再沿估计角度做径向扫），**正是因为一次扫描覆盖不了 2 维**。Zhang2026 声称"single scan"同时得到 θ 和 r，本质是把 Luo 的物理限制假装不存在。

> **你的切入点**（三选一或组合）：
> 1. 给出"轨迹可覆盖区域"的严格刻画，证明单轨迹方案的有效区域是零测集的邻域，量化偏离轨迹时的粗估计偏差；
> 2. 设计**多轨迹 / 空间填充（space-filling）beam-squint 码本**，用子阵划分或少数 RF 链在一个 OFDM 符号内真正覆盖 2 维区域；
> 3. 把轨迹设计形式化为**感知资源分配优化问题**（如 max-min FIM 行列式 over 感知区域）——两篇论文的起止点都是拍脑袋选的，没有任何优化。

---

### ★★★★★ A3. "几何补偿空间平滑"存在逻辑循环，且有效快拍数远不够

三个独立的问题叠在一起：

**(a) 逻辑循环。** 论文说 R̂_m = y_m y_m^H 是秩 1 "cannot be directly used for stable subspace decomposition"（Eq. 29 下方），于是引入相位对齐平滑。但它自己推出的结果 Eq. (38) 是：

```
E[R̃_m] = |α_m|² a^{(p0)}(a^{(p0)})^H + σ²I     ← 信号部分仍然是秩 1
```

**对齐操作的目的恰恰是让所有子阵信号分量变成同一个矢量**——即人为制造秩 1。所以它并没有解决秩亏问题，只是做了噪声平均。而经典空间平滑之所以有效，靠的正是不同子阵之间**存在相位差**（提供去相干和有效快拍），它把这个相位差主动抹掉了。

**(b) 有效快拍数不够。** N=256, M_s=128, P=129（单位平移、高度重叠）。总共只有 256 个不同的噪声采样，却要估计一个 128×128 的协方差矩阵：

```
有效快拍数 ≈ N / M_s = 256/128 = 2
```

**用 ~2 个有效快拍去估计一个 127 维的噪声子空间**，在统计上完全不成立。此时的 MUSIC 谱实际上退化成了常规波束形成器，所谓"超分辨"是模型理想化带来的假象。

**(c) 重叠子阵噪声相关，Eq. (38) 不成立。** 相邻子阵共享 127/128 个天线，噪声高度相关。`E[R̃] = |α|²aa^H + σ²I` 要求各子阵噪声独立才成立。论文那句"since D_{p,m} is a diagonal unit-modulus matrix, the white-noise covariance remains unchanged"只对**单个子阵**成立，对**重叠子阵的平均**不成立。这是一个可以明确指出的数学错误。

**(d) 孔径腰斩。** 近场测距信息来自波前曲率，其 Fisher 信息 ∝ D⁴ ~ N⁴（二次相位项 n²d²cos²θ/2r）。M_s=128 意味着有效孔径从 256 砍到 128，**测距信息量下降约 16 倍**。论文一边把孔径砍半，一边声称测距 RMSE < 1 mm 且逼近 CRLB——自相矛盾。

---

### ★★★★★ A4. 逆罪（inverse crime）：Fig. 13 是冒烟的枪

论文花了整整一页（Eq. 15–18）分析 Fresnel 二阶近似的残余相位误差 ∝ N⁴/r³，明确指出大 N、小 r 时模型失效。**然后它在 MUSIC 里照样用 Fresnel 导向矢量（Eq. 27, 31）。**

MUSIC 是模型驱动的超分辨方法，**模型失配直接产生偏差，且不随 SNR 降低**。按它自己的参数（f_c=60 GHz, λ=5 mm, d=λ/2=2.5 mm）算三阶残余相位（阵列边缘）：

| N | x_max = (N−1)d/2 | r=15 m, θ=50° 三阶相位误差 |
|---|---|---|
| 256 | 0.319 m | ≈ 0.029 rad（可忽略）✓ |
| 1024 | 1.279 m | **≈ 1.85 rad ≈ 106°**（模型彻底失效）✗ |
| 1024 (r=10 m) | 1.279 m | **≈ 4.16 rad ≈ 238°** ✗✗ |

但 **Fig. 13 显示 RMSE 从 N=64 一路单调下降到 N=1024，完全没有误差平台。** 这在物理上不可能——除非**仿真数据是用与估计器相同的 Fresnel 近似模型生成的**，模型误差因此在仿真中完全隐身。这就是典型的 inverse crime。

> **你的切入点**：用**精确球面波**重新生成数据、跑它的算法，展示 RMSE 在大 N / 小 r 出现明显误差地板，直接证伪它的 Fig. 13 和 sub-mm 精度声明。然后提出高阶/精确流形估计器或偏差校正 Newton 精化。这个攻击**易做、易复现、无法辩驳**，性价比极高。

---

### ★★★★☆ A5. 声称精度与声称复杂度互相矛盾（可精确算账）

第二阶段是 ±Δθ=1°、±Δr=1 m 窗内的**二维网格搜索**。要达到 RMSE 0.001°：

- 角度：2° 跨度 / 0.001° 步长 → ~2000 点
- 距离：2 m 跨度 / 0.001 m 步长 → ~2000 点
- N_g^loc ≈ 4×10⁶ 个网格点
- 每点计算 `a^H U_n U_n^H a`，M_s=128 → ≈ M_s² = 16384 次复乘 ≈ 6.5×10⁴ flops
- 单子载波：≈ 2.6×10¹¹ flops；再乘 |S| 个子载波

即使按 100 GFLOPS 算，**单子载波就要 2.6 秒**。而 Fig. 9 声称总时延 **3.90 ms**——**差了 3～4 个数量级**。

结论只能二选一：要么网格很粗（那 0.001° 不可能达到），要么网格很细（那 3.90 ms 是假的）。**这是一个纯算术的、无法辩解的内部矛盾。**

---

### ★★★★☆ A6. 隐藏了巨大的感知开销，导致时延对比完全失真

Eq. (9)–(11)：单 RF 链下，要重构完整的 N 维阵列快拍 y_m，需要 **T = N = 256 次顺序探测（V = I_N）**。

- Fig. 9 声称"proposed 3.90 ms ≈ CBS-Low 3.88 ms"。但 CBS-Low 只需 **2 次**波束扫描，本方案需要 **256 次**顺序测量 → **感知开销约 128 倍**，却被算成"几乎相同的时延"。
- 更糟：V = I_N 意味着**一次只开一根天线**，每次测量的阵列增益比波束成形测量低 N 倍 = **24 dB**。论文的 SNR 定义（−10 dB 到 20 dB）**从未说明是阵元级还是阵列级**。如果是阵列输出端定义，那和 CBS-Low（享有完整波束成形增益）的对比就是不公平的。
- 256 次顺序测量期间要求信道完全静止、相位完全相干——无相噪、无 CFO、无移动性。这在 60 GHz 上是很强的假设。

> **你的切入点**：建立**开销归一化（RMSE vs. 总时频资源）**的公平对比框架。这几乎必然会翻转结论。进一步：设计 **T ≪ N 的压缩/随机合并感知 + 压缩近场子空间估计**，既是新贡献，又直接暴露它的隐藏开销。

---

### ★★★★☆ A7. 基线是稻草人：从头到尾没有对比 CBS-High

Zhang2026 的全部 RMSE 曲线（Fig. 10–13）对比的是 **CBS-Low [20]**，而 Luo2024 的高精度方案 **CBS-High** 一次都没出现。

- Zhang2026 的"误差传播"批判**只对 CBS-Low 成立**。
- CBS-High 恰恰已经用**多次扫描平均 + 多载波相位差测距**缓解了这个问题，在 P=12、SNR=10 dB 时达到 RMSE_θ=0.018°、RMSE_r=0.037 m。
- 它选择攻击 Luo 最弱的变体，同时把 [22] DCD-MUSIC（**窄带方法**，它自己承认"designed for narrowband systems"）拿来在宽带场景下当基线——这是天生注定要输的对手。

其它缺失的强基线：近场 2D-ESPRIT / unitary ESPRIT（无网格、闭式）、张量分解（它自己引了 [4][5] 却不比）、ML / Newton 精化（比网格 MUSIC 更便宜且无偏）、稀疏贝叶斯 / 原子范数。

---

### ★★★★☆ A8. CRLB 推导有两个实质性问题，导致"逼近 CRLB"的说法失效

**(a) 它的 CRLB 是窄带的。** Appendix B 的导向矢量 Eq. (58) 只用了 **f_c 一个频点**，完全忽略了 W = 3 GHz 带宽和 M = 2048 个子载波。但宽带 OFDM 在**相位-频率斜率（时延域）**中携带大量绝对距离信息（c/(2W) = 5 cm 的原生距离分辨率，还没算波束成形）。

所以：**它拿一个窄带 CRLB 去当宽带系统的性能界**，然后说"我们逼近了 CRLB"。真正的宽带近场 CRLB 要低得多，两篇论文实际上都离真正的界很远。

**(b) 讨厌参数没有边缘化。** 它声称把 s_l 当作未知确定性参数，但 Eq. (59) 的 FIM 只有 2×2（仅 θ, r），**从未对 s_l 和 σ² 做 Schur 补边缘化**。正确做法应得到投影正交形式 `∂a^H Π_a^⊥ ∂a`。忽略讨厌参数 → FIM 偏大 → **CRLB 偏低（过于乐观）**。它的算法却还能"贴着"这个过于乐观的界跑——更说明结果不可信。

> **你的切入点**：推导**正确的宽带近场联合 CRLB**（空-频联合流形、精确球面波、讨厌参数边缘化），再加一个**开销归一化的界**。这本身就是一篇有分量的理论论文，而且一次性把两篇的性能声明都放在正确的尺子上量。

---

### ★★★☆☆ A9. 它把宽带系统当窄带用——最大的技术机会

这一条既是攻击点，也是**我认为最值得做的正面创新**。

- MUSIC 是**逐子载波**做的（Eq. 41），然后只用**谱的几何平均**（Eq. 44）融合。
- 几何平均是**非相干融合**——它丢掉了子载波之间的相位相干性，也就是丢掉了整个时延维度的信息。
- 一个 W = 3 GHz 的系统，被用成了 M 个独立的窄带系统。

> **你的切入点**：构造 **N×M 的空-频联合观测**，在 (天线 n, 子载波 m) 二维流形上做真正的联合超分辨：
> - 2D unitary ESPRIT / 张量（CP、Tucker）分解 → **闭式、无网格、无粗估计阶段、无搜索窗、从根本上没有任何误差传播**，复杂度 O(NM) 而不是二维网格；
> - 或相干宽带聚焦（CSSM/RSS）后统一处理。
>
> 这直接在 Zhang2026 自己声称的贡献上完胜它，同时顺手解决 A1（无粗估计→无对齐误差）、A2（不依赖轨迹覆盖）、A5（无网格→复杂度真实可控）。

---

### ★★★☆☆ A10. 唯一性证明（Appendix A）不成立于它自己的算法

- **只证了全局精确解，没证局部极值。** 实际有害的是近场特有的**角度-距离耦合脊**（θ 与 r 沿一条双曲脊互相补偿形成的长条状谷）。证明对此只字未提。
- **忽略了 mod 2π。** Eq. (56) 把相位差当作 n 的二次多项式，要求系数严格为零。但相位是模 2π 定义的，约束应是 `Φ_n(θ',r') − Φ_n(θ,r) ≡ const (mod 2π)`。d=λ/2 加上二次项，模 2π 缠绕**可以**制造真实的模糊点。这是证明的实质漏洞。
- **前提与自己的算法不符。** 证明假设噪声子空间是精确正交补，完全忽略了它自己引入的、由粗估计驱动的相位对齐扰动（A1）。**所以这个证明不适用于它自己提出的算法。**

---

### ★★☆☆☆ A11. 其它可列举的硬伤（写 related work 时的弹药）

| # | 问题 | 位置 |
|---|---|---|
| a | **贡献 1 未兑现**：声称"clarify the monotonicity and uniqueness conditions"，但全文没有任何证明，只有一句"This is guaranteed by the structure of (19) and (20)"。（附注：sinθ̃_m 关于 m 的单调性我验算过**确实成立**，导数符号 = W f₀(W+f₀)(sinθ_e−sinθ_s)/B² 恒定；但 1/r̃_m 含 cos²θ̃_m 耦合，且**单坐标单调 ≠ 二维映射适定**——这正是 A2） | Sec. III-C |
| b | **系统模型混乱**：标题写 "for Near-Field **Communications**"、正文写 "user localization"，信号模型却是**单站雷达回波**（Eq. 7）。回波双程传播路损 ∝ r⁴ 而非 r²；需要目标 RCS（从未定义）；需要全双工自干扰抵消（从未提及）。而基线 CBS-Low 是下行+用户反馈的协作方案——**两者根本不可比** | Eq. (7), Sec. V |
| c | **参数自相矛盾**：感知区域声明为 15 m ≤ r ≤ 50 m（Table II），但 Fig. 11(b) 测试了 **r = 10 m** 的用户——在区域之外 | Table II vs. Fig. 11 |
| d | **Rayleigh 距离对不上**：Fig. 2 讨论称 N=256 时 Z ≈ 230 m。按 f_c=60 GHz、d=λ/2 算 D=0.64 m，Z=2D²/λ=**163 m**；按 30 GHz 算是 325 m。都不是 230 m | Sec. III-A |
| e | **Table I 复杂度公式前后不一致**：Global 2D MUSIC 一处写 `N_g P M_s²`，另一处写 `N_g M_s²` | Sec. IV-D |
| f | **排除基线的理由牵强**："these conventional schemes do not have a unique FLOP-order expression"——这不是排除的正当理由 | Sec. IV-D |
| g | **多用户分离靠断言**：声称"each user corresponds to a unique peak subcarrier m\*_k"。但轨迹是 1 维曲线，(θ,r)→m 的映射在横向是**多对一**；同一轨迹点上的两个用户不可分；不在轨迹上的用户只被旁瓣照到。且 Fig. 12 做到 K=20 用户，正交导频/调度开销从未计入时延 | Sec. IV-C-2 |
| h | **单径 LoS**：多径下粗估计功率谱出现多峰，MUSIC 信源数未知（**信源数估计问题完全未处理**）。NLoS 路径会在不同子载波产生自己的峰 → 灾难性粗估计错误。论文只说 "extensible"，从未做 | Sec. II |
| i | ~~**无门限效应**~~ — **此条经实验 C 证伪，已撤回**。轨迹上的用户，粗估计功率谱峰非常锐利且抗噪（−10 dB 时 RMSE 仅 0.139 m），窗口捕获率在所有 SNR 下均为 100%。不存在我原先设想的门限/野值问题 | — |
| j | ~~搜索窗循环论证~~ — 同上撤回。**但实验 C 发现了更严重的问题**：第二阶段 MUSIC 精化使距离比它所要精化的粗估计**恶化 7–18 倍**（见下方 A12） | Sec. IV-C-1 |
| k | **零硬件非理想性**：TTD 有限延时范围/量化分辨率/插损、PS 量化、阵列互耦与幅相失配、相噪、CFO、移动性/多普勒——全部为零 | 全文 |

### ★★★★★ A12. 第二阶段"精化"实际上让距离变差 7–18 倍（实验 C 实测）

这是实验 C 意外发现的、比原先设想的门限效应强得多的攻击点，**直接否定论文的整个
coarse-to-fine 架构**。

用户置于轨迹上（对论文最有利），窗口捕获率全程 100%：

| 每阵元 SNR | 第一阶段 RMSE$_r$ | 第二阶段"精化"后 RMSE$_r$ | 恶化倍数 |
|---|---|---|---|
| −10 dB | 0.139 m | 1.006 m | **7.2×** |
| 0 dB | 0.077 m | 0.904 m | **11.7×** |
| +10 dB | 0.054 m | 0.695 m | **12.9×** |
| +15 dB | 0.024 m | 0.441 m | **18.4×** |

**机理**：第一阶段的距离来自频率→空间映射，在轨迹上很准；第二阶段却只能从
**单频点的波前曲率**、用约 2 个有效快拍（见 A3）恢复距离，其距离分辨率本质很差
（|S|=5 时非相干 CRLB 为 0.315 m），MUSIC 谱峰在 ±1 m 窗内游走，RMSE 趋近窗宽。

**论文 Fig. 10 展示的是相反的结论。** 就距离而言，它的"精化"阶段是**做减法**的。

这条同时反向支撑了推荐创新点：**正确的精化方向是时延域，而不是曲率域 MUSIC。**

---

## 2. Luo2024 遗留、且 Zhang2026 也没解决的问题（对你仍然开放）

1. **角度感知阶段的解耦近似缺乏有效域分析**。Luo 的 Sec. IV-B 需要"select appropriate r_mid1, r_mid2 such that ξ₂ ≈ p·2π"——**纯启发式，没有设计准则，没有失效条件分析**。这个近似只在有限的角度/距离跨度内成立。Zhang2026 直接继承了这个公式（它的 Eq. 19/20）却连提都没提。
2. **距离感知的功率谱在峰值附近是平的**（Luo 自己在 Fig. 8 下方承认），低 SNR 下噪声敏感。这是**曲率测距的物理本质**，不是算法问题。
3. **CBS-High 的多载波相位差测距有 2π 模糊**（Eq. 27），从未讨论。P 个峰值子载波的频率间隔是不规则的，模糊解算完全没做。
4. **CBS-2BS 需要第二个基站 + 严格同步 + 已知基线 L**，成本被轻描淡写。
5. **TTD 硬件可实现性**：Luo 给出 t_n ∈ [0.1889 µs, 0.2112 µs]——**约 200 ns 的真时延**。mmWave 片上 TTD 典型量级是**几百 ps**，差 2–3 个数量级。而且两篇都假设**每根天线一个 TTD**（N=256 个 TTD），成本上不可行。
6. **空间非平稳性 / 可见区域（VR）**：XL-MIMO 在 r=15 m、N=1024 时，阵列不同部分看到的用户不同。两篇都假设全阵可见且路损沿阵元恒定（α_{k,n,m} ≈ α_{k,m}）。顺带：真实近场的**幅度渐变本身携带距离信息**，两篇都丢掉了。
7. **概念层面**：所谓"联合估计消除误差传播"在信息论层面是**误导性**的。角度-距离耦合是 FIM 的性质（`Re{d_θ^H d_r}` 非零），不是算法的性质。`CRLB(r) = ‖d_θ‖²/det(J)` 本身就已经包含了耦合惩罚。联合估计消除的是**次优估计器**，不是**基本耦合**。这个论点可以直接放进你论文的 Introduction，用来重新定义问题。

---

## 3. 由此导出的创新点（按性价比排序）

### 🥇 创新点 1：真正的空-频联合无网格超分辨（打 A1 + A2 + A5 + A9）

用 N×M 空-频联合流形 + 2D unitary ESPRIT / 张量分解做闭式联合角距估计。

- **彻底没有两阶段** → 不存在任何形式的误差传播（真正兑现 Zhang2026 没兑现的承诺）
- **无网格** → 精度不受网格步长限制，复杂度 O(NM) 而非二维网格搜索
- **利用时延维度** → 把 3 GHz 带宽的距离信息真正用起来
- 直接对标它的标题和贡献，正面完胜

### 🥇 创新点 2：正确的宽带近场 CRLB + 开销归一化性能界（打 A6 + A8）

推导空-频联合、精确球面波、讨厌参数边缘化的 CRLB；再定义"给定总时频感知资源下的可达精度界"。

- 一把尺子同时量倒两篇的性能声明
- 理论贡献扎实，审稿人难以反驳
- 附带产出：证明 Zhang2026 的 sub-mm 声明违反正确的界

### 🥈 创新点 3：Beam-squint 轨迹设计的优化理论（打 A2）

把轨迹起止点从"拍脑袋"升级为优化问题：在感知区域上 max-min FIM 行列式 / 最小化最坏情况 CRLB；设计多轨迹或空间填充码本，用子阵划分在单 OFDM 符号内真正覆盖 2 维区域。

- 两篇论文的轨迹端点**全是手选的**，这块完全空白
- 可以顺带给出"单轨迹覆盖域"的严格刻画，作为对 A2 的正面理论回应

### 🥈 创新点 4：模型失配鲁棒的近场估计（打 A4）

用精确球面波生成数据（消除 inverse crime），量化 Fresnel 截断引起的**偏差地板**，提出高阶流形 / 偏差校正 / 精确模型 Newton 精化。

- **实验最容易做、结论最不可辩驳**
- 建议作为你论文的 motivating experiment：一张"精确模型 vs. Fresnel 模型下 RMSE-vs-N"的图就能立住整篇文章

### 🥉 创新点 5：单 RF 链下的压缩感知式测量设计（打 A6）

设计 T ≪ N 的随机/压缩合并矩阵 V + 压缩近场子空间估计，在保持精度的同时把感知开销从 256 次降到十几次。

- 既是实用贡献，又同时暴露 Zhang2026 隐藏的 128× 开销

### 🥉 创新点 6：硬件受限的 TTD 架构（打 A11-k + Luo-5）

有限 TTD 数量（子连接、每子阵一个 TTD）、有限延时范围、量化延时/相位下的轨迹设计与定位性能分析。

- 两篇都假设 N 个理想 TTD、~200 ns 延时范围，与真实 mmWave 硬件差 2–3 个数量级
- 实用性强，容易讲清楚故事

### 备选：多径/NLoS + 信源数未知；VR 感知定位；移动性下的联合位置-速度跟踪

---

## 4. 建议的验证路线（按顺序做，逐步积累弹药）

1. **复现 Zhang2026 的 Fig. 10 和 Fig. 13**（用它自己的 Fresnel 模型）→ 确认能复现它的曲线，建立可信基线。
2. **把数据生成换成精确球面波**（Eq. 15），估计器保持 Fresnel 不变 → 预期在 N≥512 或 r≤15 m 出现明显误差地板。**这就是 A4 的实锤。**
3. **把用户放到轨迹以外**（如 (15°, 45 m)，轨迹端点仍取 (−60°,15 m)→(60°,50 m)）→ 预期粗估计距离误差 ≫ Δr=1 m，第二阶段崩溃。**这是 A2 的实锤。**
4. **统计野值率 / 窗口捕获概率 vs. SNR** → 画出它论文里缺失的门限效应曲线。**这是 A11-i + A11-j 的实锤。**
5. **精确统计 MUSIC 网格搜索的实际 FLOPs 和墙钟时间**，与它声称的 3.90 ms 对照。**这是 A5 的实锤。**
6. **按总时频资源归一化重画 RMSE 曲线**（把 T=N=256 次顺序探测算进去），与 CBS-Low / **CBS-High** 对比。**这是 A6 + A7 的实锤。**
7. 在以上基础上，实现创新点 1 的空-频联合估计器，与全部基线对比。

---

## 5. 一句话总结

> Zhang2026 声称解决了 Luo2024 的误差传播，但它 (i) 用粗估计去构造相位对齐矩阵，把误差传播搬进了协方差矩阵；(ii) 用一维轨迹去反解二维位置，粗估计在数学上欠定；(iii) 用 ~2 个有效快拍估计 127 维噪声子空间；(iv) 在与估计器相同的近似模型上生成仿真数据，掩盖了模型失配偏差；(v) 声称的精度与声称的复杂度相差 3–4 个数量级；(vi) 隐藏了 128 倍的感知开销；(vii) 从未与 Luo2024 真正的高精度方案 CBS-High 对比。
>
> **而两篇共同留下的最大空白是：拥有 3 GHz 带宽，却都只做了非相干的窄带处理——时延维度的距离信息被完整地丢掉了。**
