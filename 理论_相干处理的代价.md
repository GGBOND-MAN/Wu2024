# 相干宽带处理的代价：损伤敏感性

> **状态**：三项损伤全部量化完成，代码 `sim/exp_g_impairments.py`。
> **结论**：**方案成立，但定时稳定度是约束性要求，必须在论文里明确写为系统指标。**

相干跨频带处理是本方案唯一比逐子载波方法多要求的东西，因此也是它唯一可能失败的地方。
本节把这个风险量化到底。

---

## 1. 理论预判

OFDM 中，振荡器相噪与载波频偏（CFO）都分解为

$$\text{公共相位误差（CPE，所有子载波相同）} \;+\; \text{ICI}$$

而 **CPE 恰好被未知复幅度 $\alpha$ 完全吸收**（估计器本来就要边缘化它）。所以 CPE 应当零代价，
只有 ICI 残差有影响。

真正的危险是**定时偏移** $\tau$：它在相位上贡献 $-2\pi f_m \tau$，
**与距离的 signature 完全相同**，因此一比一地变成距离偏差 $c\tau$。

---

## 2. 实测（SNR = 10 dB，界为 RMSE$_r$ = 2.41×10⁻⁵ m）

### 2.1 公共相位误差 —— 确认被吸收

| CPE rms | RMSE$_r$ | 相对干净 |
|---|---|---|
| 0.1 rad | 4.871×10⁻⁵ m | 1.07× |
| 0.5 rad | 4.871×10⁻⁵ m | 1.07× |
| 1.0 rad | 4.871×10⁻⁵ m | 1.07× |
| **3.0 rad** | **4.871×10⁻⁵ m** | **1.07×** |

**0.1 rad 到 3.0 rad（30 倍）给出完全相同的数值** —— 这就是 CPE 被精确吸收的证据。

> *注*：相对 CPE=0 的 1.07× 并非退化，而是 `cpe_rms>0` 时多消耗一次随机数、
> 移动了后续噪声实现所致。关键证据是 0.1 与 3.0 rad **逐位相同**。

### 2.2 ICI 残差 —— 大致线性代价

| 残余逐子载波相位 rms | RMSE$_r$ | 相对干净 |
|---|---|---|
| 0.01 rad (0.57°) | 6.32×10⁻⁵ m | 1.39× |
| 0.03 rad (1.72°) | 1.30×10⁻⁴ m | 2.87× |
| 0.10 rad (5.73°) | 3.96×10⁻⁴ m | 8.71× |
| 0.30 rad (17.2°) | 1.17×10⁻³ m | 25.8× |

**要求**：CPE 去除后的残余相位需 $\sigma_\phi \lesssim 0.01$ rad（0.57° rms）
才能保持在干净情况的 1.4 倍以内。

60 GHz 系统总积分相噪典型为 1–3° rms，而其中占主导的低频部分正是被吸收的 CPE，
残余 ICI 部分远小于此 —— **0.57° 残余是合理的**。

### 2.3 定时偏移 —— 一比一变成距离偏差

| $\tau$ | 实测偏差 | 预测 $c\tau$ | 比值 |
|---|---|---|---|
| 10⁻¹⁴ s | +3.40×10⁻⁶ m | 3.00×10⁻⁶ m | 1.13 |
| 10⁻¹³ s | +3.04×10⁻⁵ m | 3.00×10⁻⁵ m | 1.01 |
| 10⁻¹² s | +3.00×10⁻⁴ m | 3.00×10⁻⁴ m | 1.00 |
| 10⁻¹¹ s | +3.00×10⁻³ m | 3.00×10⁻³ m | **1.00** |

**完全按 $c\tau$ 走。** 要达到 10⁻⁴ m 的距离精度，需要

$$\tau < \frac{10^{-4}}{c} = 3.3\times10^{-13}\ \text{s} = 0.33\ \text{ps}$$

---

## 3. 这个要求现实吗？——两点关键区分

### 3.1 静态偏移 vs 抖动

- **静态** $\tau$ 是标定常数：用已知参考一次标定即可扣除，不构成基本限制。
- **抖动**才是真限制。相关指标是振荡器在**往返时间尺度**上的短期稳定度 ——
  $r=30$ m 时往返仅 $2r/c \approx 200$ ns。在 200 ns 尺度上，
  设计良好的 60 GHz PLL 抖动通常在数十飞秒到低皮秒量级，**0.33 ps 属于可达范围**。

### 3.2 单站共钟

本方案（与 Zhang2026 相同）采用**单站回波**模型，收发共用振荡器，
因此不存在绝对定时未知的问题，只需短期稳定度。

> ⚠️ **但下行 + 用户反馈场景（Luo2024 模型）不同**：用户时钟偏移未知，
> 距离只能确定到 $c\tau_{\rm off}$ 以内。此前设想的解法是"多加一个待估参数、
> 靠跨天线差分时延定几何"—— **但 T=2 之后已无逐天线分辨率，该解法不再适用。**
> 因此本方案目前**限定于单站共钟场景**，这一点必须在论文中明确声明，
> 不能含糊带过。下行场景需要额外机制（如往返测量 RTT），属未来工作。

---

## 4. 诚实的权衡表述

曲率类方法（Luo2024、Zhang2026、arXiv:2603.16390）**完全不需要定时精度** ——
它们本质是窄带的。本方案用亚皮秒定时稳定度，换来**四个数量级**的距离精度提升。

论文里应当把这写成一个**明确的工程权衡**，而不是回避：

> The proposed estimator trades a sub-picosecond timing-stability requirement,
> which monostatic operation makes a short-term stability specification rather
> than an absolute one, for four orders of magnitude in ranging accuracy. Common
> phase error and carrier frequency offset are absorbed by the unknown complex
> amplitude and cost nothing; only the ICI residual, which must be held below
> about 0.01 rad rms, and timing jitter enter the error budget.

这类要求在 5G NR 定位（PRS、multi-RTT）中已是成熟工程领域，**并非异常指标**。

---

## 5. 结论

| 损伤 | 影响 | 要求 |
|---|---|---|
| 公共相位误差（CPE） | **零** —— 被 $\alpha$ 吸收 | 无 |
| 载波频偏（CFO） | 经 CPE 通道 → 零；仅剩 ICI | 见下 |
| ICI / 相噪残差 | 近似线性 | $\sigma_\phi \lesssim 0.01$ rad |
| **定时抖动** | **一比一 → $c\tau$** | $\sigma_\tau \lesssim 0.33$ ps |
| 静态定时偏移 | 可标定扣除 | 无 |

**风险项已排除：方案不会因相噪或 CFO 失效。** 定时稳定度是唯一的硬约束，
且在单站共钟下退化为短期稳定度要求，工程上可达。
