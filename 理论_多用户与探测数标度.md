# 多用户：探测数如何随用户数标度

> **状态**：理论 + 数值验证完成，代码 `sim/multiuser.py`。
> **结论**：$T = 2K+1$，在 $K < 128$ 的整个范围内优于 Zhang2026 的固定 $T = N = 256$。

Zhang2026 做到 $K = 20$ 个用户（其 Fig. 7、Fig. 12），所以单目标对比只覆盖了它主张的一部分。

---

## 1. 先回答那个诱人的想法：squint 接收合并能否 $T = O(1)$？

**不能。而失败的原因本身就是一个重要发现。**

### 1.1 根本张力

Luo2024 与 Zhang2026 靠 squint 把用户**在频域分开** —— 这是它们区分用户的全部机制。
但**频率正是时延测距赖以生存的资源**。

一束斜视波束照亮某个用户的子载波数约为

$$M\cdot\frac{\text{波束宽度}}{\text{角度跨度}} = 2048\times\frac{0.398°}{120°} \approx 7\ \text{个}$$

于是每个用户只分到约 7 个音而非 2048 个，而 $\mathrm{RMSE}_r \sim c/(W_{\rm eff}\sqrt{NM_{\rm eff}})$ 随之崩溃。

> ### **分离与测距争抢同一个轴，不可兼得。**

### 1.2 数值验证（最差用户距离界，SNR = 0 dB）

| K | 全阵列 T=256 | **squint 接收 T=2** |
|---|---|---|
| 1 | 3.044×10⁻⁴ m | 3.32×10⁻³ m（**11×**） |
| 2 | 3.044×10⁻⁴ m | 8.67×10⁻³ m（**28×**） |
| 3 | 3.045×10⁻⁴ m | 6.69×10⁻² m（**220×**） |
| 5 | 3.044×10⁻⁴ m | 5.84×10⁻² m（**192×**） |
| 8 | 3.044×10⁻⁴ m | 5.81×10⁻² m（**191×**） |

**$T=O(1)$ 的想法被明确否定。** 这是个干净的负面结果，应当写进论文。

### 1.3 这个张力其实解释了整条脉络为何卡住

这一点值得放进 Introduction：

> Luo2024 和 Zhang2026 用频率去编码**空间**（斜视扫描），而频率同时是**时延测距**的资源。
> 它们的核心机制**恰好消耗掉了**本可以给出距离的那个资源。
> 所以问题不是"他们没想到时延"，而是**他们的中心机制与时延测距互斥**。

本方案通过**分离角色**化解：Stage A 花 1 次探测把频率用于空间扫描（取角度），
Stage B 把频率相干地用于测距。**同一个轴，不同的探测，不同的用途。**

---

## 2. 匹配对：$T = 2K$ 基本无损

每用户一对恒模波束，跨在该用户角度两侧：

| K | 全阵列 T=256 | 匹配对 T=2K | 损失 |
|---|---|---|---|
| 1 | 3.044×10⁻⁴ m | 3.099×10⁻⁴ m | 1.02× |
| 2 | 3.044×10⁻⁴ m | 3.121×10⁻⁴ m | 1.03× |
| 3 | 3.045×10⁻⁴ m | 3.059×10⁻⁴ m | 1.00× |
| 5 | 3.044×10⁻⁴ m | 3.072×10⁻⁴ m | 1.01× |
| 8 | 3.044×10⁻⁴ m | 3.140×10⁻⁴ m | 1.03× |

**$2K$ 不是设计缺陷，而是接近问题本身的要求**：$K$ 个幅度未知的用户张成
**$2K$ 维**的信息子空间。Zhang2026 的 $T=N$ 与 $K$ 无关，只是因为它捕获了
**整个 $N$ 维空间** —— 在 $2K < N$ 时这是浪费。

---

## 3. 探测预算对比

| K | 本方案 $T=2K+1$ | Zhang2026 $T=256$ | 优势 |
|---|---|---|---|
| 1 | 3 | 256 | **85.3×** |
| 2 | 5 | 256 | 51.2× |
| 5 | 11 | 256 | 23.3× |
| 10 | 21 | 256 | 12.2× |
| **20**（Zhang2026 上限） | **41** | 256 | **6.2×** |
| 50 | 101 | 256 | 2.5× |
| 128 | 257 | 256 | 持平 |
| 200 | 401 | 256 | 劣于 |

**交叉点 $K = 127$**：超过它，捕获整个 $N$ 维空间反而比捕获 $2K$ 个用户专属维度便宜。

这个交叉点有清晰的物理含义，应当在论文里明确给出 —— 它同时界定了本方案的适用范围，
比含糊地宣称"更省"更可信。

---

## 4. 论文中的表述建议

不要写成"我们永远更省"，而应写成：

> The proposed acquisition costs T = 2K+1 probes against a fixed T = N for
> snapshot-based refinement, and is therefore cheaper for K < N/2 -- covering the
> entire range of user counts reported in [Zhang2026], at 6.2x fewer probes even
> at its largest K = 20. The crossover at K = N/2 is not an artifact: K users
> with unknown amplitudes span a 2K-dimensional informative subspace, so
> capturing the full N-dimensional space only becomes economical once 2K exceeds N.

---

## 5. 尚未做的

上述是 **CRLB 分析**。多用户的**端到端估计器**（联合 $2K$ 参数的 Gauss-Newton、
用户配对/关联）尚未实现。单用户版已验证到 1.87–3.12× 界，多用户版预期类似，
但需要实测确认。
